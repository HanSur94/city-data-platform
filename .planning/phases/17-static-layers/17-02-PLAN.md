---
phase: 17-static-layers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/map/NoiseWmsLayer.tsx
  - frontend/components/map/FernwaermeLayer.tsx
  - frontend/components/map/DemographicsGridLayer.tsx
  - frontend/components/map/DemographicsPopup.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/components/map/MapView.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [REQ-NOISE-01, REQ-FERN-01, REQ-DEMO-01]

must_haves:
  truths:
    - "Road noise WMS overlay visible on map with LDEN/LNight toggle"
    - "Fernwaerme coverage polygons visible on map for known neighborhoods"
    - "Demographics grid cells render as choropleth with metric toggle"
    - "All three layers toggleable from sidebar"
  artifacts:
    - path: "frontend/components/map/NoiseWmsLayer.tsx"
      provides: "LUBW road noise WMS overlay with LDEN/LNight toggle"
      contains: "NoiseWmsLayer"
    - path: "frontend/components/map/FernwaermeLayer.tsx"
      provides: "Fernwaerme coverage polygon overlay"
      contains: "FernwaermeLayer"
    - path: "frontend/components/map/DemographicsGridLayer.tsx"
      provides: "Zensus 2022 100m grid choropleth"
      contains: "DemographicsGridLayer"
    - path: "frontend/components/map/DemographicsPopup.tsx"
      provides: "Popup for demographics grid cells"
      contains: "DemographicsPopup"
  key_links:
    - from: "frontend/components/sidebar/Sidebar.tsx"
      to: "frontend/app/page.tsx"
      via: "onToggleLayer callback for noise/fernwaerme/demographics"
      pattern: "onToggleLayer.*noise|fernwaerme|demographics"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/NoiseWmsLayer.tsx"
      via: "conditional render when noiseVisible"
      pattern: "NoiseWmsLayer"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/FernwaermeLayer.tsx"
      via: "conditional render when fernwaermeVisible"
      pattern: "FernwaermeLayer"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/DemographicsGridLayer.tsx"
      via: "conditional render when demographicsVisible"
      pattern: "DemographicsGridLayer"
---

<objective>
Add three frontend-only map layers: LUBW road noise WMS overlay, Fernwaerme coverage polygons, and Zensus 2022 demographics grid visualization with sidebar toggles.

Purpose: These layers require no new backend connectors -- noise uses LUBW WMS, Fernwaerme uses hardcoded polygons for known Aalen neighborhoods, and demographics uses the existing demographics layer endpoint with ZensusConnector data.
Output: Three new layer components, one popup component, sidebar toggles, MapView wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-static-layers/17-CONTEXT.md

@frontend/components/map/WmsOverlayLayer.tsx
@frontend/components/map/MapView.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/app/page.tsx
@frontend/hooks/useLayerData.ts
</context>

<interfaces>
<!-- Existing WmsOverlayLayer for reuse -->
From frontend/components/map/WmsOverlayLayer.tsx:
```tsx
interface WmsOverlayLayerProps {
  id: string;
  wmsUrl: string;
  layers: string;
  visible: boolean;
  opacity?: number;
}
export default function WmsOverlayLayer({ id, wmsUrl, layers, visible, opacity = 0.7 }: WmsOverlayLayerProps)
```

From frontend/hooks/useLayerData.ts:
```tsx
export function useLayerData(domain: string, town: string, timestamp?: Date | null)
// Returns { data: LayerResponse | null, lastFetched: Date | null, error: boolean }
```

From backend layers endpoint (already supports demographics domain):
```
GET /api/layers/demographics?town=aalen
// Returns GeoJSON FeatureCollection with demographics reading values merged into properties
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NoiseWmsLayer + FernwaermeLayer components</name>
  <files>
    frontend/components/map/NoiseWmsLayer.tsx,
    frontend/components/map/FernwaermeLayer.tsx
  </files>
  <read_first>
    frontend/components/map/WmsOverlayLayer.tsx,
    frontend/components/map/KocherLayer.tsx
  </read_first>
  <action>
    NoiseWmsLayer (frontend/components/map/NoiseWmsLayer.tsx):
    - Accept props: visible: boolean, noiseMetric: 'lden' | 'lnight' (default 'lden').
    - Use WmsOverlayLayer internally.
    - LUBW Umgebungslaermkartierung WMS URL: "https://rips-gdi.lubw.baden-wuerttemberg.de/arcgis/services/wms/UIS_0100000017200001/MapServer/WMSServer"
    - For LDEN: layers="0" (Strassenlaerm LDEN). For LNight: layers="2" (Strassenlaerm LNight).
    - Opacity 0.55 for readability over base map.
    - Color bands from WMS are pre-styled: green <55dB, yellow 55-65, orange 65-70, red 70-75, purple >75.
    - Render two WmsOverlayLayer instances: one for LDEN (visible when metric=lden), one for LNight (visible when metric=lnight). This avoids tile reload on toggle.

    FernwaermeLayer (frontend/components/map/FernwaermeLayer.tsx):
    - Accept props: visible: boolean.
    - Use react-map-gl Source + Layer for static GeoJSON polygons.
    - Hardcode 4 Aalen Fernwaerme coverage polygons as GeoJSON Feature array:
      1. Schlossaecker: approximate polygon around [10.080, 48.843] area
      2. Weisse Steige: approximate polygon around [10.095, 48.847] area
      3. Maiergasse/Talschule: approximate polygon around [10.093, 48.838] area
      4. Ostalbklinikum: approximate polygon around [10.078, 48.830] area
    - Each polygon ~300m radius approximation (4-6 vertex polygons).
    - Fill color: rgba(255, 120, 50, 0.25) with stroke rgba(255, 120, 50, 0.8), stroke-width 2.
    - Properties include: name (German), provider ("Stadtwerke Aalen").
    - Source id: "fernwaerme-coverage" to avoid conflicts.
    - Layer id: "fernwaerme-fill" and "fernwaerme-outline".
    - If not visible, return null.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "NoiseWmsLayer" frontend/components/map/NoiseWmsLayer.tsx
    - grep -q "FernwaermeLayer" frontend/components/map/FernwaermeLayer.tsx
    - grep -q "lden" frontend/components/map/NoiseWmsLayer.tsx
    - grep -q "lnight" frontend/components/map/NoiseWmsLayer.tsx
    - grep -q "fernwaerme-coverage" frontend/components/map/FernwaermeLayer.tsx
    - grep -q "Schlossaecker" frontend/components/map/FernwaermeLayer.tsx
  </acceptance_criteria>
  <done>NoiseWmsLayer renders LUBW road noise WMS with LDEN/LNight metric selection. FernwaermeLayer renders 4 hardcoded Aalen Fernwaerme coverage polygons with orange fill.</done>
</task>

<task type="auto">
  <name>Task 2: DemographicsGridLayer + DemographicsPopup</name>
  <files>
    frontend/components/map/DemographicsGridLayer.tsx,
    frontend/components/map/DemographicsPopup.tsx
  </files>
  <read_first>
    frontend/components/map/AirQualityHeatmapLayer.tsx,
    frontend/components/map/ParkingPopup.tsx,
    frontend/hooks/useLayerData.ts,
    frontend/types/geojson.ts
  </read_first>
  <action>
    DemographicsGridLayer (frontend/components/map/DemographicsGridLayer.tsx):
    - Accept props: town: string, visible: boolean, activeMetric: DemographicMetric.
    - Export type DemographicMetric = 'population' | 'age' | 'rent' | 'heating'.
    - Use useLayerData('demographics', town) to fetch demographics features.
    - The demographics endpoint returns features with properties from demographics_readings values (population, households, etc.) merged in by the layers router.
    - Since ZensusConnector currently creates a single point feature (not grid cells), AND the Zensus 2022 grid WMS exists at atlas.zensus2022.de: use the Zensus WMS for the grid overlay, plus show the point feature data in popup.
    - Render a WmsOverlayLayer for the Zensus grid:
      - WMS URL: "https://atlas.zensus2022.de/geoserver/ows"
      - Layers by metric: population="Bevoelkerung:Einwohner_100m_Gitter", age="Bevoelkerung:Durchschnittsalter_100m_Gitter", rent="Gebaeude_Wohnungen:Durchschnittliche_Wohnungsmiete_100m_Gitter", heating="Gebaeude_Wohnungen:Heizungsart_100m_Gitter"
      - Opacity 0.6.
    - If not visible, return null.
    - Source id: "demographics-grid" to avoid conflicts.

    DemographicsPopup (frontend/components/map/DemographicsPopup.tsx):
    - Accept props: feature: GeoJSON.Feature.
    - Display: population count, households count, AGS, data source.
    - Format numbers with toLocaleString('de-DE').
    - Simple card layout matching existing popup patterns (see ParkingPopup).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DemographicsGridLayer" frontend/components/map/DemographicsGridLayer.tsx
    - grep -q "DemographicsPopup" frontend/components/map/DemographicsPopup.tsx
    - grep -q "DemographicMetric" frontend/components/map/DemographicsGridLayer.tsx
    - grep -q "population" frontend/components/map/DemographicsGridLayer.tsx
    - grep -q "zensus2022" frontend/components/map/DemographicsGridLayer.tsx
  </acceptance_criteria>
  <done>DemographicsGridLayer renders Zensus 2022 WMS grid overlay with metric toggle. DemographicsPopup shows census data for clicked features.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all three layers into Sidebar + MapView + page.tsx</name>
  <files>
    frontend/components/sidebar/Sidebar.tsx,
    frontend/components/map/MapView.tsx,
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/sidebar/Sidebar.tsx,
    frontend/components/map/MapView.tsx,
    frontend/app/page.tsx
  </read_first>
  <action>
    Sidebar.tsx:
    - Add to SidebarProps.layerVisibility: roadNoise (already exists as railNoise -- but this is for ROAD noise, different layer), fernwaerme?: boolean, demographics?: boolean.
    - Wait -- railNoise already exists for Bahnlaerm. Road noise (LUBW Strassenlaerm) is a NEW separate layer. Add: roadNoise?: boolean, fernwaerme?: boolean, demographics?: boolean.
    - Add to onToggleLayer union type: 'roadNoise' | 'fernwaerme' | 'demographics'.
    - Add noiseMetric and onNoiseMetricChange props for LDEN/LNight toggle (same pattern as PollutantToggle).
    - Add demographicMetric and onDemographicMetricChange props.
    - Add LayerToggle entries:
      - Under "Ebenen" section: "Strassenlaerm (LUBW)" for roadNoise, with sub-toggle for LDEN/LNight when visible.
      - Under "Energie" section: "Fernwaerme-Netz" for fernwaerme.
      - Under "Gemeinwesen" section: "Demografie (Zensus)" for demographics, with sub-toggle for metric when visible.

    MapView.tsx:
    - Add props: roadNoiseVisible, fernwaermeVisible, demographicsVisible, noiseMetric, demographicMetric.
    - Import NoiseWmsLayer, FernwaermeLayer, DemographicsGridLayer, DemographicsPopup.
    - Add to interactiveLayerIds: 'demographics-grid-layer' (if applicable -- WMS is raster so not interactive, skip).
    - Render NoiseWmsLayer when roadNoiseVisible, pass noiseMetric.
    - Render FernwaermeLayer when fernwaermeVisible.
    - Render DemographicsGridLayer when demographicsVisible, pass demographicMetric.
    - Add 'demographics' to PopupInfo domain union and popup rendering.

    page.tsx:
    - Add to layerVisibility: roadNoise, fernwaerme, demographics with URL param keys 'road_noise', 'fernwaerme', 'demographics'.
    - Add to LAYER_KEYS mapping.
    - Add useState for noiseMetric ('lden' | 'lnight', default 'lden') and demographicMetric (DemographicMetric, default 'population').
    - Pass new visibility props and metric state to Sidebar and MapView.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "roadNoise" frontend/components/sidebar/Sidebar.tsx
    - grep -q "fernwaerme" frontend/components/sidebar/Sidebar.tsx
    - grep -q "demographics" frontend/components/sidebar/Sidebar.tsx
    - grep -q "NoiseWmsLayer" frontend/components/map/MapView.tsx
    - grep -q "FernwaermeLayer" frontend/components/map/MapView.tsx
    - grep -q "DemographicsGridLayer" frontend/components/map/MapView.tsx
    - grep -q "road_noise" frontend/app/page.tsx
    - grep -q "fernwaerme" frontend/app/page.tsx
    - grep -q "demographics" frontend/app/page.tsx
  </acceptance_criteria>
  <done>All three new layers toggleable from sidebar with sub-toggles for noise metric and demographic metric. Map renders layers conditionally. URL state persists layer visibility.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Sidebar shows toggles for Strassenlaerm, Fernwaerme, Demografie
- NoiseWmsLayer switches between LDEN and LNight WMS layers
- FernwaermeLayer renders 4 orange coverage polygons
- DemographicsGridLayer renders Zensus 2022 WMS grid
- URL params persist layer visibility state
</verification>

<success_criteria>
- Three new map layers visible and toggleable from sidebar
- Noise layer has LDEN/LNight sub-toggle
- Demographics layer has population/age/rent/heating sub-toggle
- Fernwaerme shows 4 known Aalen district heating areas
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-static-layers/17-02-SUMMARY.md`
</output>
