---
phase: 17-static-layers
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/components/map/HeatDemandLayer.tsx
  - frontend/components/map/HeatDemandPopup.tsx
  - frontend/components/map/CyclingLayer.tsx
  - frontend/components/map/CyclingPopup.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/components/sidebar/CyclingLegend.tsx
  - frontend/components/sidebar/HeatDemandLegend.tsx
  - frontend/components/map/MapView.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [REQ-HEAT-01, REQ-CYCLE-01]

must_haves:
  truths:
    - "Buildings colored by heat demand kWh/m2/y on map (blue->red gradient)"
    - "Cycling infrastructure road segments colored by type (separated->none gradient)"
    - "Click on heat demand building shows kWh/m2/y value and class"
    - "Click on cycling road segment shows infrastructure type and road name"
    - "Both layers toggleable from sidebar with legends"
  artifacts:
    - path: "frontend/components/map/HeatDemandLayer.tsx"
      provides: "Heat demand building coloring layer"
      contains: "HeatDemandLayer"
    - path: "frontend/components/map/HeatDemandPopup.tsx"
      provides: "Popup for heat demand buildings"
      contains: "HeatDemandPopup"
    - path: "frontend/components/map/CyclingLayer.tsx"
      provides: "Cycling infrastructure road segments layer"
      contains: "CyclingLayer"
    - path: "frontend/components/map/CyclingPopup.tsx"
      provides: "Popup for cycling road segments"
      contains: "CyclingPopup"
    - path: "frontend/components/sidebar/CyclingLegend.tsx"
      provides: "Legend for cycling infrastructure colors"
      contains: "CyclingLegend"
    - path: "frontend/components/sidebar/HeatDemandLegend.tsx"
      provides: "Legend for heat demand colors"
      contains: "HeatDemandLegend"
  key_links:
    - from: "frontend/components/map/HeatDemandLayer.tsx"
      to: "/api/layers/energy?town=aalen&feature_type=heat_demand"
      via: "useLayerData or fetch with feature_type filter"
      pattern: "feature_type.*heat_demand"
    - from: "frontend/components/map/CyclingLayer.tsx"
      to: "/api/layers/infrastructure?town=aalen&source=cycling"
      via: "useLayerData or fetch with source filter"
      pattern: "source.*cycling"
    - from: "frontend/components/sidebar/Sidebar.tsx"
      to: "frontend/app/page.tsx"
      via: "onToggleLayer for heatDemand/cycling"
      pattern: "heatDemand|cycling"
---

<objective>
Create frontend layers for heat demand building coloring and cycling infrastructure road segments, with popups, legends, sidebar toggles, and MapView wiring.

Purpose: These layers visualize the data produced by Plan 01's backend connectors. Heat demand colors buildings by energy efficiency. Cycling infrastructure highlights road safety for cyclists.
Output: Two layer components, two popup components, two legend components, sidebar/MapView/page wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-static-layers/17-CONTEXT.md
@.planning/phases/17-static-layers/17-01-SUMMARY.md

@frontend/components/map/MapView.tsx
@frontend/components/map/TrafficFlowLayer.tsx
@frontend/components/map/ParkingLayer.tsx
@frontend/components/map/ParkingPopup.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/components/sidebar/TrafficLegend.tsx
@frontend/app/page.tsx
@frontend/hooks/useLayerData.ts
</context>

<interfaces>
<!-- From Plan 01 backend output (connectors create these feature properties) -->

HeatDemandConnector features (domain="energy", feature_type="heat_demand"):
```
properties: {
  feature_type: "heat_demand",
  source: "KEA-BW Waermeatlas (simulated)",
  heat_demand_kwh_m2_y: number,  // 30-350
  heat_class: "blue" | "light_blue" | "green" | "yellow" | "orange" | "red"
}
geometry: Point
```

CyclingInfraConnector features (domain="infrastructure", feature_type="cycling"):
```
properties: {
  feature_type: "cycling",
  source: "OpenStreetMap (cycling)",
  infra_type: "separated" | "lane" | "advisory" | "shared" | "none",
  road_name: string | null
}
geometry: LineString
```

<!-- Existing layers endpoint supports feature_type and source query params -->
GET /api/layers/energy?town=aalen  (filter heat_demand features by feature_type in frontend)
GET /api/layers/infrastructure?town=aalen&source=cycling  (source filter supported by layers router)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HeatDemandLayer + CyclingLayer components with popups and legends</name>
  <files>
    frontend/components/map/HeatDemandLayer.tsx,
    frontend/components/map/HeatDemandPopup.tsx,
    frontend/components/map/CyclingLayer.tsx,
    frontend/components/map/CyclingPopup.tsx,
    frontend/components/sidebar/CyclingLegend.tsx,
    frontend/components/sidebar/HeatDemandLegend.tsx
  </files>
  <read_first>
    frontend/components/map/TrafficFlowLayer.tsx,
    frontend/components/map/ParkingLayer.tsx,
    frontend/components/map/ParkingPopup.tsx,
    frontend/components/sidebar/TrafficLegend.tsx,
    frontend/hooks/useLayerData.ts
  </read_first>
  <action>
    HeatDemandLayer (frontend/components/map/HeatDemandLayer.tsx):
    - Accept props: town: string, visible: boolean.
    - Fetch energy layer data using useLayerData('energy', town).
    - Filter features client-side where properties.feature_type === 'heat_demand'.
    - Render as Source (type="geojson", id="heat-demand") + Layer (type="circle").
    - Circle color by heat_class property: blue=#2166ac, light_blue=#67a9cf, green=#5aae61, yellow=#fee08b, orange=#f46d43, red=#d73027.
    - Circle radius: 8 at zoom 14, scaled with interpolation.
    - If not visible, return null.

    HeatDemandPopup (frontend/components/map/HeatDemandPopup.tsx):
    - Accept props: feature: GeoJSON.Feature.
    - Display: "Waermebedarf: {heat_demand_kwh_m2_y} kWh/m2/a", heat_class as colored badge.
    - Source attribution line at bottom.

    CyclingLayer (frontend/components/map/CyclingLayer.tsx):
    - Accept props: town: string, visible: boolean.
    - Fetch infrastructure layer data: custom fetch to /api/layers/infrastructure?town={town}&source=cycling (use inline fetchLayer pattern like BusPositionLayer, or useLayerData with custom URL).
    - Actually, useLayerData fetches /api/layers/{domain}?town={town}. For source filtering, use a custom fetch. Create a simple useEffect + fetch approach.
    - Render as Source (type="geojson", id="cycling-infra") + Layer (type="line").
    - Line color by infra_type: separated=#1a7c28 (dark green), lane=#5cb85c (light green), advisory=#f0ad4e (yellow), shared=#e67e22 (orange), none=#d9534f (red).
    - Line width: 3 for separated/lane, 2 for advisory/shared/none.
    - If not visible, return null.

    CyclingPopup (frontend/components/map/CyclingPopup.tsx):
    - Accept props: feature: GeoJSON.Feature.
    - Display: road_name (or "Unbenannt"), infra_type as German label with colored dot.
    - German labels: separated="Getrennter Radweg", lane="Radfahrstreifen", advisory="Schutzstreifen", shared="Gemeinsamer Weg", none="Keine Radinfrastruktur".

    CyclingLegend (frontend/components/sidebar/CyclingLegend.tsx):
    - Follow TrafficLegend pattern. Show 5 color entries with German labels.

    HeatDemandLegend (frontend/components/sidebar/HeatDemandLegend.tsx):
    - Follow TrafficLegend pattern. Show 6 color entries: <50 blau, 50-100 hellblau, 100-150 gruen, 150-200 gelb, 200-250 orange, >250 rot. Unit: kWh/m2/a.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "HeatDemandLayer" frontend/components/map/HeatDemandLayer.tsx
    - grep -q "HeatDemandPopup" frontend/components/map/HeatDemandPopup.tsx
    - grep -q "CyclingLayer" frontend/components/map/CyclingLayer.tsx
    - grep -q "CyclingPopup" frontend/components/map/CyclingPopup.tsx
    - grep -q "CyclingLegend" frontend/components/sidebar/CyclingLegend.tsx
    - grep -q "HeatDemandLegend" frontend/components/sidebar/HeatDemandLegend.tsx
    - grep -q "heat-demand" frontend/components/map/HeatDemandLayer.tsx
    - grep -q "cycling-infra" frontend/components/map/CyclingLayer.tsx
    - grep -q "infra_type" frontend/components/map/CyclingLayer.tsx
  </acceptance_criteria>
  <done>HeatDemandLayer renders building heat demand as colored circles. CyclingLayer renders road segments colored by infrastructure type. Both have popups and sidebar legends.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HeatDemand + Cycling into Sidebar + MapView + page.tsx</name>
  <files>
    frontend/components/sidebar/Sidebar.tsx,
    frontend/components/map/MapView.tsx,
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/sidebar/Sidebar.tsx,
    frontend/components/map/MapView.tsx,
    frontend/app/page.tsx
  </read_first>
  <action>
    NOTE: Plan 02 already modified Sidebar.tsx, MapView.tsx, page.tsx to add roadNoise/fernwaerme/demographics. This task adds heatDemand and cycling on top of those changes. Read the files AFTER Plan 02 execution to get the current state.

    Sidebar.tsx:
    - Add to layerVisibility: heatDemand?: boolean, cycling?: boolean.
    - Add to onToggleLayer union: 'heatDemand' | 'cycling'.
    - Under "Energie" section, add LayerToggle: "Waermebedarf (KEA-BW)" for heatDemand.
    - Under "Verkehr" section, add LayerToggle: "Radinfrastruktur (OSM)" for cycling.
    - In Legend section: show HeatDemandLegend when heatDemand visible, CyclingLegend when cycling visible.
    - Import HeatDemandLegend and CyclingLegend.

    MapView.tsx:
    - Add props: heatDemandVisible?: boolean, cyclingVisible?: boolean.
    - Import HeatDemandLayer, HeatDemandPopup, CyclingLayer, CyclingPopup.
    - Add to interactiveLayerIds: 'heat-demand-points', 'cycling-infra-lines'.
    - Render HeatDemandLayer when heatDemandVisible.
    - Render CyclingLayer when cyclingVisible.
    - Add 'heatDemand' and 'cycling' to PopupInfo domain union.
    - Add domain detection in onClick: layerId === 'heat-demand-points' -> 'heatDemand', layerId === 'cycling-infra-lines' -> 'cycling'.
    - Add popup rendering for heatDemand and cycling domains.

    page.tsx:
    - Add to layerVisibility: heatDemand, cycling with URL param keys 'heat_demand', 'cycling'.
    - Add to LAYER_KEYS mapping: heatDemand: 'heat_demand', cycling: 'cycling'.
    - Pass heatDemandVisible and cyclingVisible to MapView.
    - Pass to Sidebar.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "heatDemand" frontend/components/sidebar/Sidebar.tsx
    - grep -q "cycling" frontend/components/sidebar/Sidebar.tsx
    - grep -q "HeatDemandLayer" frontend/components/map/MapView.tsx
    - grep -q "CyclingLayer" frontend/components/map/MapView.tsx
    - grep -q "HeatDemandPopup" frontend/components/map/MapView.tsx
    - grep -q "CyclingPopup" frontend/components/map/MapView.tsx
    - grep -q "heat_demand" frontend/app/page.tsx
    - grep -q "cycling" frontend/app/page.tsx
  </acceptance_criteria>
  <done>Heat demand and cycling layers fully wired: sidebar toggles, MapView rendering, popups, legends, URL state persistence. All 5 Phase 17 layers are now operational.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Sidebar shows Waermebedarf toggle under Energie and Radinfrastruktur toggle under Verkehr
- HeatDemandLayer renders colored circles for building heat demand
- CyclingLayer renders colored road segments for cycling infrastructure
- Popups show relevant data on click
- Legends appear when layers are active
- URL params persist heat_demand and cycling layer visibility
</verification>

<success_criteria>
- Heat demand buildings rendered with 6-tier color scheme (blue to red)
- Cycling roads rendered with 5-tier color scheme (dark green to red)
- Click popups show heat demand value/class and cycling type/road name
- Sidebar legends provide color key for both layers
- All 5 static layers from Phase 17 are toggleable and functional
</success_criteria>

<output>
After completion, create `.planning/phases/17-static-layers/17-03-SUMMARY.md`
</output>
