---
phase: 18-data-transparency
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - frontend/components/sidebar/LayerToggle.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/components/dashboard/KpiTile.tsx
  - frontend/components/dashboard/DashboardPanel.tsx
  - frontend/components/map/TrafficFlowPopup.tsx
  - frontend/components/map/FeaturePopup.tsx
  - frontend/components/map/EnergyPopup.tsx
  - frontend/components/map/TrafficPopup.tsx
  - frontend/components/map/AutobahnPopup.tsx
  - frontend/components/map/CommunityPopup.tsx
  - frontend/components/map/RoadworksPopup.tsx
  - frontend/components/map/KocherPopup.tsx
  - frontend/components/map/ParkingPopup.tsx
  - frontend/components/map/BusPopup.tsx
  - frontend/components/map/EvChargingPopup.tsx
  - frontend/components/map/SolarPopup.tsx
  - frontend/components/map/HeatDemandPopup.tsx
  - frontend/components/map/CyclingPopup.tsx
  - frontend/components/map/DemographicsPopup.tsx
  - frontend/hooks/useLayerMetadata.ts
autonomous: true
requirements: [REQ-TRANS-01, REQ-TRANS-02, REQ-TRANS-03, REQ-TRANS-04, REQ-TRANS-05]

must_haves:
  truths:
    - "Every layer toggle in sidebar shows a colored data type badge (LIVE/SCRAPED/INTERPOLATED/MODELED/STATIC)"
    - "Info icon on each layer toggle opens a metadata popover with source, data type, last updated, interval, license"
    - "Stale layers show a warning indicator when last update > 2x expected interval"
    - "KPI widget footers show source abbreviation + timestamp + data type badge"
    - "Every feature popup includes a Datenquelle section with source name, link, badge, timestamp"
  artifacts:
    - path: "frontend/components/sidebar/LayerToggle.tsx"
      provides: "Layer toggle with DataTypeBadge and info icon metadata popover"
      contains: "DataTypeBadge"
    - path: "frontend/components/dashboard/KpiTile.tsx"
      provides: "KPI tile with source footer"
      contains: "sourceAbbrev"
    - path: "frontend/hooks/useLayerMetadata.ts"
      provides: "Hook fetching /api/metadata/layers for last_updated + stale detection"
      exports: ["useLayerMetadata"]
    - path: "frontend/components/map/DataSourceSection.tsx"
      provides: "Reusable data source section (from Plan 01)"
  key_links:
    - from: "frontend/components/sidebar/LayerToggle.tsx"
      to: "frontend/lib/layer-metadata.ts"
      via: "LAYER_METADATA lookup by layerKey"
      pattern: "LAYER_METADATA"
    - from: "frontend/hooks/useLayerMetadata.ts"
      to: "/api/metadata/layers"
      via: "fetch on mount"
      pattern: "metadata/layers"
    - from: "frontend/components/sidebar/LayerToggle.tsx"
      to: "frontend/hooks/useLayerMetadata.ts"
      via: "stale detection from last_updated vs interval"
      pattern: "useLayerMetadata"
    - from: "frontend/components/map/TrafficFlowPopup.tsx"
      to: "frontend/components/map/DataSourceSection.tsx"
      via: "DataSourceSection at bottom of popup"
      pattern: "DataSourceSection"
---

<objective>
Wire data transparency into all existing UI: sidebar layer toggles get badges + info icon, KPI tiles get source footers, and all feature popups get Datenquelle sections. Add stale data warning.

Purpose: Every user-facing layer, widget, and popup now communicates its data provenance, freshness, and source.
Output: Modified sidebar, dashboard, and all popup components with data transparency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-transparency/18-CONTEXT.md
@.planning/phases/18-data-transparency/18-01-SUMMARY.md

@frontend/components/sidebar/LayerToggle.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/components/dashboard/KpiTile.tsx
@frontend/components/dashboard/DashboardPanel.tsx
@frontend/components/map/FeaturePopup.tsx
@frontend/components/map/TrafficFlowPopup.tsx
@frontend/components/ui/DataTypeBadge.tsx (from Plan 01)
@frontend/components/map/DataSourceSection.tsx (from Plan 01)
@frontend/lib/layer-metadata.ts (from Plan 01)

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/lib/layer-metadata.ts:
```typescript
export type DataType = 'LIVE' | 'SCRAPED' | 'INTERPOLATED' | 'MODELED' | 'STATIC';
export interface LayerMetadataEntry {
  layerKey: string;
  sourceName: string;
  sourceUrl: string;
  dataType: DataType;
  updateIntervalSeconds: number | null;
  license: string;
  licenseUrl: string;
  sourceAbbrev: string;
}
export const LAYER_METADATA: Record<string, LayerMetadataEntry>;
export const DATA_TYPE_COLORS: Record<DataType, string>;
```

From frontend/components/ui/DataTypeBadge.tsx:
```typescript
export function DataTypeBadge({ dataType, size }: { dataType: DataType; size?: 'sm' | 'md' }): JSX.Element;
```

From frontend/components/map/DataSourceSection.tsx:
```typescript
export function DataSourceSection({ sourceName, sourceUrl, dataType, timestamp }: {
  sourceName: string; sourceUrl: string; dataType: DataType; timestamp?: string | null;
}): JSX.Element;
```

From frontend/components/sidebar/LayerToggle.tsx:
```typescript
interface LayerToggleProps {
  id: string; label: string; checked: boolean;
  onCheckedChange: (checked: boolean) => void;
  freshnessError?: boolean;
}
```

From frontend/components/dashboard/KpiTile.tsx:
```typescript
interface KpiTileProps {
  domain: string; label: string; icon: React.ReactNode;
  value: string | null; unit: string; trend: number | null;
  active: boolean; onSelect: (domain: string) => void;
  children?: React.ReactNode;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar layer toggles with badges, info icon, stale warning, and useLayerMetadata hook</name>
  <read_first>
    - frontend/components/sidebar/LayerToggle.tsx (current toggle component)
    - frontend/components/sidebar/Sidebar.tsx (all layer toggle instances)
    - frontend/lib/layer-metadata.ts (LAYER_METADATA registry from Plan 01)
    - frontend/components/ui/DataTypeBadge.tsx (badge component from Plan 01)
    - frontend/components/ui/popover.tsx (shadcn Popover for metadata popup)
  </read_first>
  <files>
    frontend/hooks/useLayerMetadata.ts
    frontend/components/sidebar/LayerToggle.tsx
    frontend/components/sidebar/Sidebar.tsx
  </files>
  <action>
    1. Create `frontend/hooks/useLayerMetadata.ts`:
       - Fetch GET `/api/metadata/layers?town={town}` on mount (town from env or default "aalen")
       - Return `{ layerMeta: Record<string, { lastUpdated: string | null; isStale: boolean }>, loading: boolean }`
       - Stale detection: if `lastUpdated` exists and `updateIntervalSeconds` exists in LAYER_METADATA, mark stale when `(now - lastUpdated) > 2 * updateIntervalSeconds * 1000`
       - Re-fetch every 60 seconds via setInterval
       - Export as named export `useLayerMetadata`

    2. Modify `frontend/components/sidebar/LayerToggle.tsx`:
       - Add new optional props: `layerKey?: string`, `dataType?: DataType`, `isStale?: boolean`, `metadata?: LayerMetadataEntry`
       - After the label text, render `<DataTypeBadge dataType={dataType} size="sm" />` if dataType is provided
       - Add an info icon (lucide `Info` icon, 14px, text-muted-foreground) next to the Switch
       - On info icon click (stopPropagation to not trigger toggle), open a shadcn `Popover` showing:
         - Source: `{metadata.sourceName}` as link to sourceUrl
         - Typ: DataTypeBadge
         - Aktualisierung: human-readable interval ("alle 5 Min", "alle 15 Min", "einmalig" for null)
         - Lizenz: `{metadata.license}` as link to licenseUrl
         - Letztes Update: timestamp or "Keine Daten"
       - If `isStale` is true, show an orange warning triangle icon (lucide `AlertTriangle`, 12px) next to the label with title="Daten moeglicherweise veraltet" (per REQ-TRANS-03)
       - Keep backward-compatible: if no layerKey/dataType provided, render as before

    3. Modify `frontend/components/sidebar/Sidebar.tsx`:
       - Import `useLayerMetadata` hook, call at top of component
       - Import `LAYER_METADATA` from `@/lib/layer-metadata`
       - For each `<LayerToggle>`, add `layerKey`, `dataType={LAYER_METADATA[key]?.dataType}`, `isStale={layerMeta[key]?.isStale}`, `metadata={LAYER_METADATA[key]}` props
       - Map layer keys: transit, busPosition, airQuality, water, kocher, floodHazard (no metadata — skip), railNoise (no metadata — skip), lubwEnv (no metadata — skip), roadNoise, traffic, trafficFlow, autobahn, mobiData, cycling, energy, solarGlow, fernwaerme, heatDemand, schools, healthcare, parks, waste, evCharging, roadworks, solarPotential, parking, demographics
       - Layers without metadata entries (floodHazard, railNoise, lubwEnv, cadastral, hillshade, buildings3d) get no badge/info — they render as before
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && grep -q "DataTypeBadge" frontend/components/sidebar/LayerToggle.tsx && grep -q "useLayerMetadata" frontend/hooks/useLayerMetadata.ts && grep -q "LAYER_METADATA" frontend/components/sidebar/Sidebar.tsx && grep -q "isStale" frontend/components/sidebar/LayerToggle.tsx && grep -q "Popover" frontend/components/sidebar/LayerToggle.tsx && grep -q "AlertTriangle" frontend/components/sidebar/LayerToggle.tsx && echo "sidebar OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DataTypeBadge" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "useLayerMetadata" frontend/hooks/useLayerMetadata.ts
    - grep -q "isStale" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "AlertTriangle" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "Popover" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "LAYER_METADATA" frontend/components/sidebar/Sidebar.tsx
    - grep -q "metadata/layers" frontend/hooks/useLayerMetadata.ts
    - grep -q "Aktualisierung" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "Lizenz" frontend/components/sidebar/LayerToggle.tsx
  </acceptance_criteria>
  <done>Every layer toggle shows a colored data type badge. Info icon opens metadata popover with 5 fields (source, type, interval, license, last updated). Stale layers show orange warning triangle. useLayerMetadata hook fetches freshness from backend.</done>
</task>

<task type="auto">
  <name>Task 2: KPI widget footers and popup DataSourceSection wiring</name>
  <read_first>
    - frontend/components/dashboard/KpiTile.tsx (current KPI tile)
    - frontend/components/dashboard/DashboardPanel.tsx (KPI tile instantiation with domain props)
    - frontend/components/map/DataSourceSection.tsx (from Plan 01)
    - frontend/lib/layer-metadata.ts (LAYER_METADATA, sourceAbbrev)
    - frontend/components/map/TrafficFlowPopup.tsx (popup pattern)
    - frontend/components/map/FeaturePopup.tsx (popup pattern)
    - frontend/components/map/EnergyPopup.tsx
    - frontend/components/map/KocherPopup.tsx
    - frontend/components/map/ParkingPopup.tsx
    - frontend/components/map/BusPopup.tsx
    - frontend/components/map/EvChargingPopup.tsx
    - frontend/components/map/SolarPopup.tsx
    - frontend/components/map/HeatDemandPopup.tsx
    - frontend/components/map/CyclingPopup.tsx
    - frontend/components/map/DemographicsPopup.tsx
    - frontend/components/map/AutobahnPopup.tsx
    - frontend/components/map/CommunityPopup.tsx
    - frontend/components/map/RoadworksPopup.tsx
    - frontend/components/map/TrafficPopup.tsx
  </read_first>
  <files>
    frontend/components/dashboard/KpiTile.tsx
    frontend/components/dashboard/DashboardPanel.tsx
    frontend/components/map/TrafficFlowPopup.tsx
    frontend/components/map/FeaturePopup.tsx
    frontend/components/map/EnergyPopup.tsx
    frontend/components/map/TrafficPopup.tsx
    frontend/components/map/AutobahnPopup.tsx
    frontend/components/map/CommunityPopup.tsx
    frontend/components/map/RoadworksPopup.tsx
    frontend/components/map/KocherPopup.tsx
    frontend/components/map/ParkingPopup.tsx
    frontend/components/map/BusPopup.tsx
    frontend/components/map/EvChargingPopup.tsx
    frontend/components/map/SolarPopup.tsx
    frontend/components/map/HeatDemandPopup.tsx
    frontend/components/map/CyclingPopup.tsx
    frontend/components/map/DemographicsPopup.tsx
  </files>
  <action>
    1. Modify `frontend/components/dashboard/KpiTile.tsx` (per REQ-TRANS-04):
       - Add optional props: `sourceAbbrev?: string`, `sourceTimestamp?: string`, `dataType?: DataType`
       - Below the children slot (or after trend arrow), render a footer div when sourceAbbrev is provided:
         `<div className="flex items-center gap-1.5 text-[11px] text-muted-foreground mt-1">`
           `{sourceAbbrev} . {sourceTimestamp ?? '--:--'} . <DataTypeBadge dataType={dataType} size="sm" />`
         `</div>`
       - Format: "SMARD . 14:30 . LIVE" per CONTEXT.md decision

    2. Modify `frontend/components/dashboard/DashboardPanel.tsx`:
       - Import `LAYER_METADATA` from `@/lib/layer-metadata`
       - Import `useLayerMetadata` from `@/hooks/useLayerMetadata`
       - Call useLayerMetadata at top of component
       - For each KpiTile, pass sourceAbbrev, sourceTimestamp (from layerMeta[key]?.lastUpdated formatted as HH:mm), dataType from LAYER_METADATA
       - Domain-to-layer mapping: aqi->airQuality, weather->water (DWD), transit->transit, traffic->traffic, energy->energy, demographics->demographics, parking->parking
       - KocherGaugeWidget: pass water data as before (no footer change needed — it's a custom widget)

    3. Add `<DataSourceSection>` to ALL popup components (per REQ-TRANS-05):
       - For each popup file, import DataSourceSection and LAYER_METADATA
       - At the bottom of each popup's JSX (before closing div), add:
         ```
         <DataSourceSection
           sourceName={LAYER_METADATA['{layerKey}'].sourceName}
           sourceUrl={LAYER_METADATA['{layerKey}'].sourceUrl}
           dataType={LAYER_METADATA['{layerKey}'].dataType}
           timestamp={props.measured_at ?? props.fetched_at ?? null}
         />
         ```
       - Layer key mapping per popup:
         - TrafficFlowPopup -> 'trafficFlow'
         - FeaturePopup (AQI branch) -> 'airQuality', (transit branch) -> 'transit'
         - EnergyPopup -> 'energy'
         - TrafficPopup -> 'traffic'
         - AutobahnPopup -> 'autobahn'
         - CommunityPopup -> determine from feature properties (schools/healthcare/parks/waste)
         - RoadworksPopup -> 'roadworks'
         - KocherPopup -> 'kocher'
         - ParkingPopup -> 'parking'
         - BusPopup -> 'busPosition'
         - EvChargingPopup -> 'evCharging'
         - SolarPopup -> 'solarGlow'
         - HeatDemandPopup -> 'heatDemand'
         - CyclingPopup -> 'cycling'
         - DemographicsPopup -> 'demographics'
       - For timestamp: use `props.measured_at` or `props.fetched_at` or `props.last_updated` from feature properties (whatever the popup already has access to). If none available, pass null.
       - Do NOT break existing popup functionality — only ADD the DataSourceSection at the end.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && grep -q "sourceAbbrev" frontend/components/dashboard/KpiTile.tsx && grep -q "LAYER_METADATA" frontend/components/dashboard/DashboardPanel.tsx && grep -q "DataSourceSection" frontend/components/map/TrafficFlowPopup.tsx && grep -q "DataSourceSection" frontend/components/map/FeaturePopup.tsx && grep -q "DataSourceSection" frontend/components/map/EnergyPopup.tsx && grep -q "DataSourceSection" frontend/components/map/KocherPopup.tsx && grep -q "DataSourceSection" frontend/components/map/ParkingPopup.tsx && grep -q "DataSourceSection" frontend/components/map/BusPopup.tsx && grep -q "DataSourceSection" frontend/components/map/EvChargingPopup.tsx && grep -q "DataSourceSection" frontend/components/map/TrafficPopup.tsx && grep -q "DataSourceSection" frontend/components/map/AutobahnPopup.tsx && grep -q "DataSourceSection" frontend/components/map/RoadworksPopup.tsx && grep -q "DataSourceSection" frontend/components/map/SolarPopup.tsx && grep -q "DataSourceSection" frontend/components/map/HeatDemandPopup.tsx && grep -q "DataSourceSection" frontend/components/map/CyclingPopup.tsx && grep -q "DataSourceSection" frontend/components/map/DemographicsPopup.tsx && echo "all wired"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sourceAbbrev" frontend/components/dashboard/KpiTile.tsx
    - grep -q "dataType" frontend/components/dashboard/KpiTile.tsx
    - grep -q "LAYER_METADATA" frontend/components/dashboard/DashboardPanel.tsx
    - grep -q "DataSourceSection" frontend/components/map/TrafficFlowPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/FeaturePopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/EnergyPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/KocherPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/ParkingPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/BusPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/EvChargingPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/HeatDemandPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/CyclingPopup.tsx
    - grep -q "DataSourceSection" frontend/components/map/DemographicsPopup.tsx
    - grep -q "Datenquelle" frontend/components/map/DataSourceSection.tsx
  </acceptance_criteria>
  <done>KPI tiles show "SOURCE . HH:mm . BADGE" footers. All 15 popup components include Datenquelle section with source link, data type badge, and timestamp. Stale data warning visible on sidebar toggles when last_updated exceeds 2x interval.</done>
</task>

</tasks>

<verification>
- Open sidebar: every layer toggle shows a colored badge (LIVE green, SCRAPED yellow, etc.)
- Click info icon on any layer: popover shows source name+link, data type, interval, license, last updated
- Dashboard KPI tiles show footer format: "SMARD . 14:30 . LIVE"
- Click any map feature: popup includes "Datenquelle" section at bottom
- If a connector hasn't fetched in >2x its interval, the sidebar toggle shows orange warning triangle
- `pnpm build` succeeds without TypeScript errors
</verification>

<success_criteria>
- All ~25 data layers have visible data type badges in sidebar
- Metadata popover accessible via info icon on every layer toggle
- Stale warning appears when last_updated > 2x expected interval
- All KPI widget tiles show source footer in "ABBREV . TIME . BADGE" format
- All 15 popup components include DataSourceSection with source, link, badge, timestamp
- No regressions in existing layer toggle, KPI tile, or popup functionality
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-transparency/18-02-SUMMARY.md`
</output>
