---
phase: 08-community-infrastructure-connectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/geojson.py
  - backend/app/routers/layers.py
  - backend/app/connectors/overpass_community.py
  - backend/app/connectors/overpass_roadworks.py
  - backend/app/connectors/__init__.py
  - backend/tests/connectors/test_overpass_community.py
  - backend/tests/connectors/test_overpass_roadworks.py
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, INFR-01]

must_haves:
  truths:
    - "OverpassCommunityConnector fetches schools, healthcare, parks, and waste POIs from Overpass API and upserts them as features with correct category discriminator"
    - "OverpassRoadworksConnector fetches highway=construction features and upserts them as infrastructure domain with category=roadwork"
    - "/api/layers/community returns 200 with GeoJSON FeatureCollection"
    - "/api/layers/infrastructure returns 200 with GeoJSON FeatureCollection"
  artifacts:
    - path: "backend/app/connectors/overpass_community.py"
      provides: "Community POI connector for schools, healthcare, parks, waste"
      contains: "class OverpassCommunityConnector"
    - path: "backend/app/connectors/overpass_roadworks.py"
      provides: "Roadworks connector for OSM highway=construction"
      contains: "class OverpassRoadworksConnector"
    - path: "backend/app/schemas/geojson.py"
      provides: "community and infrastructure in VALID_DOMAINS"
      contains: "community"
    - path: "backend/tests/connectors/test_overpass_community.py"
      provides: "Unit tests for category mapping"
  key_links:
    - from: "backend/app/connectors/overpass_community.py"
      to: "backend/app/connectors/base.py"
      via: "BaseConnector inheritance + upsert_feature()"
      pattern: "class OverpassCommunityConnector\\(BaseConnector\\)"
    - from: "backend/app/routers/layers.py"
      to: "backend/app/schemas/geojson.py"
      via: "VALID_DOMAINS validation"
      pattern: "community.*infrastructure"
---

<objective>
Backend foundation for Phase 8: register community and infrastructure domains, create Overpass API connectors for all community POIs (schools, healthcare, parks, waste) and roadworks, with unit tests.

Purpose: Establishes the backend data pipeline for 5 of 8 Phase 8 requirements (COMM-01 through COMM-04 and INFR-01). All use the features-only connector pattern.
Output: Two new connectors, domain registration, layers.py query branches, test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-community-infrastructure-connectors/08-CONTEXT.md
@.planning/phases/08-community-infrastructure-connectors/08-RESEARCH.md

@backend/app/connectors/base.py
@backend/app/connectors/lubw_wfs.py
@backend/app/connectors/__init__.py
@backend/app/schemas/geojson.py
@backend/app/routers/layers.py
@backend/app/config.py

<interfaces>
<!-- Key contracts from existing codebase -->

From backend/app/connectors/base.py:
- BaseConnector ABC with: fetch(), normalize(), run(), persist(), upsert_feature()
- upsert_feature(source_id, domain, geometry_wkt, properties) — upserts to features table
- _update_staleness() — records last successful fetch

From backend/app/schemas/geojson.py:
- VALID_DOMAINS: frozenset = frozenset({"air_quality", "transit", "weather", "water", "energy", "traffic"})
- CONNECTOR_ATTRIBUTION: dict[str, dict[str, str]] — maps connector class name to attribution

From backend/app/config.py:
- Town.bbox with lat_min, lon_min, lat_max, lon_max attributes
- ConnectorConfig with connector_class, poll_interval_seconds, enabled, config dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain registration + OverpassCommunityConnector + tests</name>
  <files>
    backend/app/schemas/geojson.py
    backend/app/routers/layers.py
    backend/app/connectors/overpass_community.py
    backend/app/connectors/__init__.py
    backend/tests/connectors/test_overpass_community.py
  </files>
  <read_first>
    backend/app/schemas/geojson.py
    backend/app/routers/layers.py
    backend/app/connectors/base.py
    backend/app/connectors/lubw_wfs.py
    backend/app/connectors/__init__.py
    backend/app/config.py
  </read_first>
  <behavior>
    - Test: OverpassCommunityConnector maps amenity=school to category="school", domain="community"
    - Test: OverpassCommunityConnector maps amenity=kindergarten to category="school", domain="community"
    - Test: OverpassCommunityConnector maps amenity=pharmacy/hospital/doctors/clinic/dentist to category="healthcare"
    - Test: OverpassCommunityConnector maps leisure=park/playground/sports_centre/pitch to category="park"
    - Test: OverpassCommunityConnector maps amenity=recycling/waste_disposal to category="waste"
    - Test: Way elements use center.lat/center.lon correctly (not element.lat/lon)
    - Test: Elements with missing coordinates are skipped
    - Test: source_id format is "osm:{type}:{id}"
  </behavior>
  <action>
    1. In geojson.py: Add "community" and "infrastructure" to VALID_DOMAINS frozenset. Add CONNECTOR_ATTRIBUTION entries:
       - "OverpassCommunityConnector": {"source_name": "OpenStreetMap contributors", "license": "ODbL 1.0", "url": "https://www.openstreetmap.org/copyright"}
       - "OverpassRoadworksConnector": {"source_name": "OpenStreetMap contributors", "license": "ODbL 1.0", "url": "https://www.openstreetmap.org/copyright"}
       - "LadesaeulenConnector": {"source_name": "Bundesnetzagentur", "license": "CC BY 4.0", "url": "https://www.bundesnetzagentur.de"}
       - "SolarPotentialConnector": {"source_name": "LUBW Baden-Wuerttemberg", "license": "UIS-Nutzungsvereinbarung", "url": "https://www.lubw.baden-wuerttemberg.de"}

    2. In layers.py: Add query branches for domain "community" and "infrastructure" — plain features query with no reading join (same as the generic features-only pattern used for energy when MaStR features are queried). The query should be: SELECT from features WHERE domain IN ('community') or domain IN ('infrastructure') filtered by town bbox.

    3. Create backend/app/connectors/overpass_community.py:
       - Class OverpassCommunityConnector(BaseConnector)
       - Uses the CONSOLIDATED single-query approach (one Overpass request for all community POIs)
       - Overpass query template:
         ```
         [out:json][timeout:60];
         (
           node["amenity"~"school|kindergarten|pharmacy|hospital|doctors|clinic|dentist|recycling|waste_disposal"]({bbox});
           way["amenity"~"school|kindergarten|pharmacy|hospital|doctors|clinic|dentist|recycling|waste_disposal"]({bbox});
           node["leisure"~"park|playground|sports_centre|pitch"]({bbox});
           way["leisure"~"park|playground|sports_centre|pitch"]({bbox});
         );
         out center;
         ```
       - bbox from self.town.bbox: f"{bbox.lat_min},{bbox.lon_min},{bbox.lat_max},{bbox.lon_max}"
       - Category mapping dict: school/kindergarten->"school", pharmacy/hospital/doctors/clinic/dentist->"healthcare", park/playground/sports_centre/pitch->"park", recycling/waste_disposal->"waste"
       - Coordinate extraction: lat = element.get("lat") or (element.get("center") or {}).get("lat") — handles both node and way elements
       - Skip elements with None lat or lon
       - source_id = f"osm:{element['type']}:{element['id']}"
       - Properties: name, amenity/leisure tag, address (addr:street + addr:housenumber), opening_hours, category
       - domain = "community"
       - normalize() returns [] (ABC requirement)
       - run() calls fetch, iterates elements, upserts, then _update_staleness()

    4. Register OverpassCommunityConnector in __init__.py imports.

    5. Create tests/connectors/test_overpass_community.py:
       - Mock the httpx POST response with a fixture containing sample Overpass JSON (mix of node and way elements with various amenity/leisure tags)
       - Test each category mapping
       - Test way element center coordinate extraction
       - Test missing coordinate skip
       - Test source_id format
       - Use monkeypatch or unittest.mock to mock httpx.AsyncClient
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_overpass_community.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "community" backend/app/schemas/geojson.py
    - grep -q "infrastructure" backend/app/schemas/geojson.py
    - grep -q "OverpassCommunityConnector" backend/app/connectors/overpass_community.py
    - grep -q "class OverpassCommunityConnector" backend/app/connectors/overpass_community.py
    - grep -q "upsert_feature" backend/app/connectors/overpass_community.py
    - grep -q "test_school_category\|test_healthcare\|test_park\|test_waste" backend/tests/connectors/test_overpass_community.py
  </acceptance_criteria>
  <done>
    OverpassCommunityConnector maps all 4 community categories correctly. VALID_DOMAINS includes community and infrastructure. CONNECTOR_ATTRIBUTION has all Phase 8 connectors. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OverpassRoadworksConnector + tests</name>
  <files>
    backend/app/connectors/overpass_roadworks.py
    backend/app/connectors/__init__.py
    backend/tests/connectors/test_overpass_roadworks.py
  </files>
  <read_first>
    backend/app/connectors/base.py
    backend/app/connectors/overpass_community.py
  </read_first>
  <behavior>
    - Test: OverpassRoadworksConnector queries highway=construction within town bbox
    - Test: Features upserted with domain="infrastructure", category="roadwork"
    - Test: Properties include name and highway construction details from OSM tags
    - Test: Empty results (no roadworks) handled gracefully — run() completes without error
  </behavior>
  <action>
    1. Create backend/app/connectors/overpass_roadworks.py:
       - Class OverpassRoadworksConnector(BaseConnector)
       - Overpass query: [out:json][timeout:25]; (node["highway"="construction"]({bbox}); way["highway"="construction"]({bbox});); out center;
       - Same coordinate extraction pattern as OverpassCommunityConnector
       - source_id = f"osm:{element['type']}:{element['id']}"
       - domain = "infrastructure"
       - Properties: name (from tags.name or "Baustelle"), category="roadwork", address, note (from tags.note or tags.description)
       - normalize() returns []
       - run() calls fetch, iterates, upserts, _update_staleness()

    2. Register in __init__.py

    3. Create tests/connectors/test_overpass_roadworks.py:
       - Mock httpx response with sample highway=construction elements
       - Test domain is "infrastructure" and category is "roadwork"
       - Test empty result set handled gracefully
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_overpass_roadworks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class OverpassRoadworksConnector" backend/app/connectors/overpass_roadworks.py
    - grep -q "infrastructure" backend/app/connectors/overpass_roadworks.py
    - grep -q "roadwork" backend/app/connectors/overpass_roadworks.py
    - grep -q "test_" backend/tests/connectors/test_overpass_roadworks.py
  </acceptance_criteria>
  <done>
    OverpassRoadworksConnector fetches highway=construction from Overpass, upserts to infrastructure domain with category=roadwork. Tests pass including empty-result case.
  </done>
</task>

</tasks>

<verification>
cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_overpass_community.py tests/connectors/test_overpass_roadworks.py -x -q
</verification>

<success_criteria>
- VALID_DOMAINS includes "community" and "infrastructure"
- CONNECTOR_ATTRIBUTION has entries for all 4 Phase 8 connector classes
- layers.py handles community and infrastructure domain queries
- OverpassCommunityConnector correctly maps all 4 categories (school, healthcare, park, waste)
- OverpassRoadworksConnector writes infrastructure domain with roadwork category
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-community-infrastructure-connectors/08-01-SUMMARY.md`
</output>
