---
phase: 08-community-infrastructure-connectors
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified: []
autonomous: false
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, INFR-01, INFR-02, INFR-03, INFR-04]

must_haves:
  truths:
    - "All 8 Phase 8 requirements are visually verified on the running application"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 8 community and infrastructure features on the running application.

Purpose: Confirms that all 8 requirements are met with real data visible on the map and interactive popups working correctly.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-community-infrastructure-connectors/08-CONTEXT.md
@.planning/phases/08-community-infrastructure-connectors/08-01-SUMMARY.md
@.planning/phases/08-community-infrastructure-connectors/08-02-SUMMARY.md
@.planning/phases/08-community-infrastructure-connectors/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start services and trigger connectors</name>
  <files></files>
  <read_first>
    towns/aalen.yaml
    docker-compose.yml
  </read_first>
  <action>
    1. Ensure Docker services are running: docker-compose up -d
    2. Wait for FastAPI to be healthy
    3. Trigger Phase 8 connectors manually if scheduler hasn't run them yet:
       - curl POST to /api/connectors/trigger/OverpassCommunityConnector (or equivalent health endpoint)
       - Alternatively, wait for the scheduler poll cycle
    4. Verify backend endpoints return data:
       - curl http://localhost:8000/api/layers/community?town=aalen — should return GeoJSON with features
       - curl http://localhost:8000/api/layers/infrastructure?town=aalen — should return GeoJSON with features
    5. Verify frontend is accessible at http://localhost:4000
  </action>
  <verify>
    <automated>curl -sf http://localhost:8000/api/layers/community?town=aalen | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Community features: {len(d.get(\"features\",[]))}')"</automated>
  </verify>
  <acceptance_criteria>
    - curl -sf http://localhost:8000/api/layers/community?town=aalen | grep -q "FeatureCollection"
    - curl -sf http://localhost:8000/api/layers/infrastructure?town=aalen | grep -q "FeatureCollection"
  </acceptance_criteria>
  <done>
    Both /api/layers/community and /api/layers/infrastructure return non-empty GeoJSON FeatureCollections. Frontend is accessible.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 8 requirements</name>
  <files></files>
  <action>Human visually verifies all 8 Phase 8 requirements on the running application at http://localhost:4000. Each requirement is tested by toggling the corresponding sidebar layer and clicking features to verify popups.</action>
  <what-built>
    Community and infrastructure data layers with interactive popups, sidebar toggle groups, and URL state persistence. Covers: waste/recycling locations (COMM-01), schools/Kitas (COMM-02), healthcare facilities (COMM-03), parks/playgrounds (COMM-04), roadworks (INFR-01), EV charging stations (INFR-02), solar potential WMS overlay (INFR-03, conditional), existing solar on buildings via EnergyLayer (INFR-04).
  </what-built>
  <how-to-verify>
    Open http://localhost:4000 in browser.

    **COMM-01 -- Waste/Recycling:**
    1. In sidebar under "Gemeinwesen", toggle on "Wertstoffhoefe"
    2. Verify brown circles appear on the map for Wertstoffhof/recycling locations
    3. Click a brown circle -- popup should show name and opening hours (if available)

    **COMM-02 -- Schools/Kitas:**
    4. Toggle on "Schulen & Kitas"
    5. Verify blue circles appear for schools and kindergartens
    6. Click a school -- popup shows name, address, category "Schule/Kita"

    **COMM-03 -- Healthcare:**
    7. Toggle on "Gesundheit"
    8. Verify red circles for pharmacies, doctors, hospitals
    9. Click a facility -- popup shows name, address, opening hours where available

    **COMM-04 -- Parks/Playgrounds:**
    10. Toggle on "Parks & Spielplaetze"
    11. Verify green circles for parks, playgrounds, sports facilities
    12. Click a park -- popup shows name and category

    **INFR-01 -- Roadworks:**
    13. In sidebar under "Infrastruktur", toggle on "Baustellen (OSM)"
    14. Orange circles may appear (OSM roadwork data is sparse). If no data: acceptable per CONTEXT.md

    **INFR-02 -- EV Charging:**
    15. Toggle on "E-Ladesaeulen (BNetzA)"
    16. Verify purple circles for EV charging stations in Ostalbkreis
    17. Click a station -- popup shows operator, address, plug types, power (kW), number of charging points

    **INFR-03 -- Solar Potential:**
    18. Toggle on "Solarpotenzial Daecher"
    19. If WMS is available: raster overlay should appear showing roof solar potential
    20. If WMS is not available: toggle should have no visible effect (graceful deferral -- acceptable per CONTEXT.md)

    **INFR-04 -- Existing Solar Installations:**
    21. Under "Energie" group, verify EnergyLayer still renders solar_rooftop features in yellow/amber
    22. These should be distinct from the solar potential raster (if visible)

    **Cross-cutting:**
    23. Toggle all layers on simultaneously -- verify no rendering conflicts or performance issues
    24. Zoom in/out -- verify clustering activates at city scale, individual points at street scale
    25. Copy URL with layers active -- paste in new tab -- verify same layers are active (URL state)
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — requires human visual verification"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms all 8 requirements pass or provides specific issue list
  </acceptance_criteria>
  <done>User types "approved" confirming all Phase 8 requirements are met, or provides specific issues for gap closure.</done>
  <resume-signal>Type "approved" to confirm all requirements pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All 8 Phase 8 requirements visually confirmed by human reviewer.
</verification>

<success_criteria>
- User confirms COMM-01 through COMM-04 and INFR-01 through INFR-04 are met
- Or: user provides specific issue list for gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/08-community-infrastructure-connectors/08-04-SUMMARY.md`
</output>
