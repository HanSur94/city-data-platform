---
phase: 08-community-infrastructure-connectors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/connectors/ladesaeulen.py
  - backend/app/connectors/solar_potential.py
  - backend/app/connectors/__init__.py
  - backend/tests/connectors/test_ladesaeulen.py
  - towns/aalen.yaml
autonomous: true
requirements: [INFR-02, INFR-03, INFR-04]

must_haves:
  truths:
    - "LadesaeulenConnector downloads BNetzA CSV, parses German comma decimals, filters by Ostalbkreis, and upserts EV charging stations as infrastructure features"
    - "SolarPotentialConnector exists as a conditional stub (enabled: false) that attempts LUBW WMS GetCapabilities and defers gracefully if unavailable"
    - "INFR-04 is addressed by existing Phase 7 EnergyLayer solar_rooftop classification — no new connector needed"
    - "aalen.yaml has entries for all Phase 8 connectors"
  artifacts:
    - path: "backend/app/connectors/ladesaeulen.py"
      provides: "BNetzA EV charging CSV connector"
      contains: "class LadesaeulenConnector"
    - path: "backend/app/connectors/solar_potential.py"
      provides: "Conditional LUBW solar WMS connector"
      contains: "class SolarPotentialConnector"
    - path: "backend/tests/connectors/test_ladesaeulen.py"
      provides: "Unit tests for CSV parsing and Ostalbkreis filtering"
    - path: "towns/aalen.yaml"
      provides: "All Phase 8 connector registrations"
      contains: "OverpassCommunityConnector"
  key_links:
    - from: "backend/app/connectors/ladesaeulen.py"
      to: "backend/app/connectors/base.py"
      via: "BaseConnector inheritance + upsert_feature()"
      pattern: "class LadesaeulenConnector\\(BaseConnector\\)"
    - from: "towns/aalen.yaml"
      to: "backend/app/connectors/"
      via: "connector_class registration"
      pattern: "LadesaeulenConnector"
---

<objective>
LadesaeulenConnector for BNetzA EV charging CSV data, SolarPotentialConnector stub for conditional LUBW WMS, and aalen.yaml wiring for all Phase 8 connectors.

Purpose: Completes backend connector coverage for infrastructure requirements (INFR-02, INFR-03, INFR-04). INFR-04 is already handled by Phase 7 EnergyLayer's solar_rooftop classification.
Output: Two new connector files, test suite, aalen.yaml with all 8 Phase 8 connector entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-community-infrastructure-connectors/08-CONTEXT.md
@.planning/phases/08-community-infrastructure-connectors/08-RESEARCH.md

@backend/app/connectors/base.py
@backend/app/connectors/mastr.py
@backend/app/connectors/__init__.py
@towns/aalen.yaml

<interfaces>
<!-- Key contracts -->

From backend/app/connectors/base.py:
- BaseConnector(town, config, session) with upsert_feature(source_id, domain, geometry_wkt, properties)
- _update_staleness() for last fetch tracking

From backend/app/connectors/mastr.py (24h cache pattern):
- Downloads large file, caches locally with mtime check
- Filters by geographic area (Kreis)
- normalize() returns [] (features-only)

From towns/aalen.yaml:
- connectors: list of {connector_class, poll_interval_seconds, enabled, config: {}}

BNetzA CSV columns (semicolon-separated, UTF-8, comma decimals):
- Betreiber, Strasse, Hausnummer, Postleitzahl, Ort, Bundesland, Kreis/kreisfreie Stadt
- Breitengrad, Laengengrad (comma decimal: "48,8357")
- Anschlussleistung (kW, comma decimal)
- Art der Ladeeinrichtung (Normalladen/Schnellladen)
- Anzahl Ladepunkte
- Steckertypen1..4, P1 [kW]..P4 [kW]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LadesaeulenConnector with CSV parsing + tests</name>
  <files>
    backend/app/connectors/ladesaeulen.py
    backend/app/connectors/__init__.py
    backend/tests/connectors/test_ladesaeulen.py
  </files>
  <read_first>
    backend/app/connectors/base.py
    backend/app/connectors/mastr.py
    backend/app/connectors/__init__.py
  </read_first>
  <behavior>
    - Test: CSV rows with comma decimals in Breitengrad/Laengengrad are parsed correctly (e.g., "48,8357" -> 48.8357)
    - Test: Rows filtered by "Kreis/kreisfreie Stadt" == kreis_filter config value (default "Ostalbkreis")
    - Test: Rows with missing or empty coordinates are skipped
    - Test: Plug types aggregated from Steckertypen1..4 into a list, empty strings excluded
    - Test: Power values (P1..P4) parsed with comma decimal normalization
    - Test: Features upserted with domain="infrastructure", category="ev_charging"
    - Test: Properties include operator, address, plug_types list, max_power_kw, num_charging_points, charging_type
  </behavior>
  <action>
    1. Create backend/app/connectors/ladesaeulen.py:
       - Class LadesaeulenConnector(BaseConnector)
       - fetch(): Download CSV from BNetzA URL. Use the page-scraping fallback strategy:
         a. First try known URL pattern: f"https://data.bundesnetzagentur.de/Bundesnetzagentur/DE/Fachthemen/ElektrizitaetundGas/E-Mobilitaet/Ladesaeulenregister_BNetzA_{date}.csv"
         b. Cache CSV locally at /tmp/ladesaeulenregister.csv with 24h mtime check (same pattern as MastrConnector)
         c. If cached file is fresh, read from cache. If stale or missing, download fresh.
         d. If the dated URL 404s, fall back to scraping the download page to find current CSV link. Use the latest confirmed URL as default: "https://data.bundesnetzagentur.de/Bundesnetzagentur/DE/Fachthemen/ElektrizitaetundGas/E-Mobilitaet/Ladesaeulenregister_BNetzA_2026-03-25.csv"
       - Parse CSV: csv.DictReader with delimiter=";", encoding="utf-8"
       - Helper: _parse_german_float(val: str) -> float | None — does val.replace(",", ".").strip() then float(), returns None on ValueError/empty
       - Filter: row["Kreis/kreisfreie Stadt"] == self.config.get("kreis_filter", "Ostalbkreis")
       - Coordinate extraction: _parse_german_float(row["Breitengrad"]), _parse_german_float(row["Laengengrad"]) — skip if None
       - Plug types: collect non-empty values from Steckertypen1..Steckertypen4
       - Power: collect non-None _parse_german_float values from P1 [kW]..P4 [kW], take max
       - source_id = f"bnetza:{row.get('Postleitzahl', '')}:{row.get('Strasse', '')}:{row.get('Hausnummer', '')}"
         (or use a row index if address is not unique)
       - domain = "infrastructure"
       - Properties: operator (Betreiber), address (Strasse + Hausnummer + Postleitzahl + Ort), plug_types (list), max_power_kw, num_charging_points (Anzahl Ladepunkte), charging_type (Art der Ladeeinrichtung), category="ev_charging"
       - normalize() returns []
       - run(): load/download CSV, parse rows, filter by Kreis, upsert features, _update_staleness()

    2. Register in __init__.py

    3. Create tests/connectors/test_ladesaeulen.py:
       - Create a small CSV fixture string (5-10 rows) with semicolon delimiter, comma decimals, mix of Ostalbkreis and other Kreis values
       - Mock httpx download to return the fixture CSV bytes
       - Mock file cache (use tmp_path)
       - Test comma decimal parsing
       - Test Kreis filtering (only Ostalbkreis rows upserted)
       - Test plug type aggregation
       - Test missing coordinate skip
       - Test properties structure in upsert_feature call
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_ladesaeulen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class LadesaeulenConnector" backend/app/connectors/ladesaeulen.py
    - grep -q "replace.*,.*\." backend/app/connectors/ladesaeulen.py
    - grep -q "Ostalbkreis\|kreis_filter" backend/app/connectors/ladesaeulen.py
    - grep -q "ev_charging" backend/app/connectors/ladesaeulen.py
    - grep -q "test_" backend/tests/connectors/test_ladesaeulen.py
  </acceptance_criteria>
  <done>
    LadesaeulenConnector parses BNetzA CSV with German comma decimals, filters by Kreis, upserts EV stations with plug type details. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: SolarPotentialConnector stub + aalen.yaml wiring</name>
  <files>
    backend/app/connectors/solar_potential.py
    backend/app/connectors/__init__.py
    towns/aalen.yaml
  </files>
  <read_first>
    backend/app/connectors/base.py
    backend/app/connectors/lubw_wfs.py
    towns/aalen.yaml
  </read_first>
  <action>
    1. Create backend/app/connectors/solar_potential.py:
       - Class SolarPotentialConnector(BaseConnector) — conditional/stub connector
       - run(): Attempt WMS GetCapabilities at candidate LUBW RIPS-GDI URL pattern:
         "https://rips-gdi.lubw.baden-wuerttemberg.de/arcgis/services/wms/UIS_0100000003700001/MapServer/WMSServer?REQUEST=GetCapabilities&SERVICE=WMS"
       - If response is 200 and contains valid WMS XML with layer names, log available layers and store the WMS base URL in properties
       - If response is non-200 or no valid layers, log warning "Solar potential WMS not available — INFR-03 deferred" and return without upserting
       - normalize() returns []
       - This is intentionally a probe — no features are upserted. The WMS URL (if found) is used by the frontend WmsOverlayLayer directly.
       - Store the discovered WMS URL (or empty string) in a class attribute for the frontend solar toggle to conditionally render

    2. Register in __init__.py

    3. Update towns/aalen.yaml — add all Phase 8 connector entries at the end of the connectors list:
       ```yaml
       - connector_class: OverpassCommunityConnector
         poll_interval_seconds: 86400
         enabled: true
         config:
           attribution: "OpenStreetMap contributors, ODbL"

       - connector_class: OverpassRoadworksConnector
         poll_interval_seconds: 3600
         enabled: true
         config:
           attribution: "OpenStreetMap contributors, ODbL"

       - connector_class: LadesaeulenConnector
         poll_interval_seconds: 86400
         enabled: true
         config:
           kreis_filter: "Ostalbkreis"
           attribution: "Bundesnetzagentur, CC BY 4.0"

       - connector_class: SolarPotentialConnector
         poll_interval_seconds: 86400
         enabled: false
         config:
           wms_url: ""
           attribution: "LUBW Baden-Wuerttemberg"
       ```
       Note: SolarPotentialConnector enabled: false — only enabled if WMS endpoint is confirmed at runtime.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && python -c "from app.connectors.solar_potential import SolarPotentialConnector; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class SolarPotentialConnector" backend/app/connectors/solar_potential.py
    - grep -q "OverpassCommunityConnector" towns/aalen.yaml
    - grep -q "OverpassRoadworksConnector" towns/aalen.yaml
    - grep -q "LadesaeulenConnector" towns/aalen.yaml
    - grep -q "SolarPotentialConnector" towns/aalen.yaml
    - grep -q "enabled: false" towns/aalen.yaml
  </acceptance_criteria>
  <done>
    SolarPotentialConnector probes LUBW WMS and defers gracefully. aalen.yaml has all 4 new Phase 8 connector entries with correct config. INFR-04 is covered by existing Phase 7 EnergyLayer solar_rooftop classification — no additional connector needed.
  </done>
</task>

</tasks>

<verification>
cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_ladesaeulen.py -x -q && python -c "from app.connectors.solar_potential import SolarPotentialConnector; from app.connectors.ladesaeulen import LadesaeulenConnector; print('All imports OK')"
</verification>

<success_criteria>
- LadesaeulenConnector parses German comma-decimal CSV, filters by Kreis, upserts with plug type details
- SolarPotentialConnector exists as conditional probe with graceful deferral
- aalen.yaml has all 4 Phase 8 connector entries
- INFR-04 requires no new work (existing EnergyLayer solar_rooftop classification)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-community-infrastructure-connectors/08-02-SUMMARY.md`
</output>
