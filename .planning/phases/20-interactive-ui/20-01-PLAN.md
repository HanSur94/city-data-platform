---
phase: 20-interactive-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/page.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/components/dashboard/DashboardPanel.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/map/LegendOverlay.tsx
autonomous: true
requirements: [REQ-UI-02, REQ-UI-03, REQ-UI-04]

must_haves:
  truths:
    - "Left sidebar collapses/expands with smooth animation when toggle button clicked"
    - "Right dashboard panel collapses/expands with smooth animation when toggle button clicked"
    - "Map fills full width when both sidebars are collapsed"
    - "Legend button on map toggles a legend overlay panel"
    - "Map can be rotated via right-click drag and has compass control"
  artifacts:
    - path: "frontend/components/map/LegendOverlay.tsx"
      provides: "Floating legend overlay component on map"
      min_lines: 30
    - path: "frontend/app/page.tsx"
      provides: "Collapsible sidebar state and layout changes"
      contains: "leftCollapsed"
    - path: "frontend/components/map/MapView.tsx"
      provides: "Map rotation enabled and NavigationControl"
      contains: "dragRotate"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/sidebar/Sidebar.tsx"
      via: "collapsed prop and onToggle callback"
      pattern: "collapsed.*onToggle"
    - from: "frontend/app/page.tsx"
      to: "frontend/components/dashboard/DashboardPanel.tsx"
      via: "collapsed prop and onToggle callback"
      pattern: "collapsed.*onToggle"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/LegendOverlay.tsx"
      via: "child component inside Map"
      pattern: "LegendOverlay"
---

<objective>
Add collapsible sidebars (left and right), map rotation/pitch controls, and a map legend overlay button.

Purpose: These are layout and map control improvements that make the interface more flexible — users can maximize map space and interact with the 3D view more naturally.
Output: Both sidebars collapse with toggle buttons, map supports rotation with compass, legend overlay toggles on map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-interactive-ui/20-CONTEXT.md
@frontend/app/page.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/components/dashboard/DashboardPanel.tsx
@frontend/components/map/MapView.tsx
@frontend/components/sidebar/AQILegend.tsx
@frontend/components/sidebar/TrafficLegend.tsx
@frontend/components/sidebar/WaterLegend.tsx
@frontend/components/sidebar/EnergyLegend.tsx
@frontend/components/sidebar/HeatDemandLegend.tsx
@frontend/components/sidebar/CyclingLegend.tsx
@frontend/components/sidebar/TransitLegend.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapsible sidebars and map rotation</name>
  <read_first>
    - frontend/app/page.tsx (full layout structure, sidebar/dashboard integration)
    - frontend/components/sidebar/Sidebar.tsx (current sidebar structure, desktop/mobile behavior)
    - frontend/components/dashboard/DashboardPanel.tsx (current dashboard panel structure)
    - frontend/components/map/MapView.tsx (Map component props, current NavigationControl setup)
  </read_first>
  <files>
    frontend/app/page.tsx,
    frontend/components/sidebar/Sidebar.tsx,
    frontend/components/dashboard/DashboardPanel.tsx,
    frontend/components/map/MapView.tsx
  </files>
  <action>
    **Collapsible Sidebars (per REQ-UI-02):**

    1. In `page.tsx`, add two state variables:
       - `const [leftCollapsed, setLeftCollapsed] = useState(false)`
       - `const [rightCollapsed, setRightCollapsed] = useState(false)`

    2. Pass `collapsed` and `onToggle` props to both `Sidebar` and `DashboardPanel`.

    3. In `Sidebar.tsx`:
       - Add `collapsed?: boolean` and `onToggle?: () => void` to `SidebarProps`.
       - Wrap the desktop `<aside>` in a container with CSS transition: `transition-all duration-300 ease-in-out`.
       - When collapsed: width 0, overflow hidden, transform translateX(-100%).
       - Add a toggle button: a small button with `ChevronLeft`/`ChevronRight` icon positioned at the right edge of the sidebar (absolute, right-0, translate-x-full). Use `z-30` so it floats above the map. The button should always be visible regardless of collapsed state.
       - Button appearance: 28px wide, 48px tall, rounded-r-md, bg-white, border, shadow-sm, centered chevron icon.

    4. In `DashboardPanel.tsx`:
       - Add `collapsed?: boolean` and `onToggle?: () => void` to `DashboardPanelProps`.
       - Same transition pattern but using `translateX(100%)` when collapsed (slides right).
       - Toggle button at the left edge (absolute, left-0, translate-x-[-100%]). Use `ChevronRight`/`ChevronLeft` from lucide-react.
       - Button appearance: same as sidebar but rounded-l-md.

    5. In `page.tsx` layout: wrap each sidebar in a `<div>` with `relative` so the toggle buttons position correctly. The `<main>` flex layout handles width redistribution automatically when sidebar widths go to 0.

    **Map Rotation (per REQ-UI-04):**

    6. In `MapView.tsx`, on the `<Map>` component:
       - Ensure `dragRotate` is NOT disabled (it is enabled by default in MapLibre but check if there is any prop disabling it).
       - The Map already has `pitch: 45` in initialViewState — rotation should already work via right-click drag and Ctrl+drag for pitch.
       - Add `<NavigationControl position="top-right" showCompass={true} showZoom={true} visualizePitch={true} />` from `react-map-gl/maplibre`. Import it.
       - This gives the user a compass indicator, zoom buttons, and a way to reset bearing by clicking the compass.

    Do NOT add any animation or transition to the map itself — the flex layout handles resizing automatically.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "leftCollapsed" frontend/app/page.tsx
    - grep -q "rightCollapsed" frontend/app/page.tsx
    - grep -q "collapsed" frontend/components/sidebar/Sidebar.tsx
    - grep -q "collapsed" frontend/components/dashboard/DashboardPanel.tsx
    - grep -q "onToggle" frontend/components/sidebar/Sidebar.tsx
    - grep -q "onToggle" frontend/components/dashboard/DashboardPanel.tsx
    - grep -q "NavigationControl" frontend/components/map/MapView.tsx
    - grep -q "transition" frontend/components/sidebar/Sidebar.tsx
    - grep -q "transition" frontend/components/dashboard/DashboardPanel.tsx
  </acceptance_criteria>
  <done>Both sidebars have toggle buttons that collapse/expand them with smooth CSS transitions. Map has NavigationControl with compass, zoom, and pitch visualization. Map fills available width when sidebars collapse.</done>
</task>

<task type="auto">
  <name>Task 2: Map legend overlay</name>
  <read_first>
    - frontend/components/map/MapView.tsx (where to place the overlay)
    - frontend/components/sidebar/AQILegend.tsx (legend component pattern)
    - frontend/components/sidebar/TrafficLegend.tsx (legend component pattern)
    - frontend/components/sidebar/Sidebar.tsx (which legends are shown for which layers, lines 493-506)
  </read_first>
  <files>
    frontend/components/map/LegendOverlay.tsx,
    frontend/components/map/MapView.tsx
  </files>
  <action>
    **Legend Overlay (per REQ-UI-03):**

    1. Create `LegendOverlay.tsx`:
       - A floating panel component that sits on top of the map.
       - Props: `layerVisibility` (same type as in MapView), `activePollutant`, `noiseMetric`, `demographicMetric`, and a `trafficFlowVisible` boolean.
       - Renders a button (bottom-left of map, z-20) with a `ListOrdered` or `Map` icon from lucide-react and text "Legende".
       - On click, expands a panel (max-height 60vh, overflow-y auto, bg-white/95, backdrop-blur, rounded-lg, shadow-lg, border) showing the active layer legends.
       - Include the same legends currently in Sidebar.tsx lines 493-506: AQILegend (always), TransitLegend (always), WaterLegend (if water visible), TrafficLegend (if traffic/autobahn/trafficFlow visible), EnergyLegend (if energy visible), HeatDemandLegend (if heatDemand visible), CyclingLegend (if cycling visible).
       - Close button (X icon) in top-right corner of the panel.
       - Panel position: absolute bottom-12 left-4 (above the button).

    2. In `MapView.tsx`:
       - Import and render `<LegendOverlay>` inside the outer `<div className="relative w-full h-full">` (outside the `<Map>` component, alongside the historical timestamp overlay).
       - Pass the necessary layer visibility props. These are already available as props on MapView.

    Do NOT remove the legends from Sidebar.tsx — they remain there for when sidebar is visible. The overlay is an independent toggle for when sidebars are collapsed or user wants quick legend access.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/components/map/LegendOverlay.tsx
    - grep -q "LegendOverlay" frontend/components/map/MapView.tsx
    - grep -q "AQILegend" frontend/components/map/LegendOverlay.tsx
    - grep -q "TrafficLegend" frontend/components/map/LegendOverlay.tsx
    - grep -q "layerVisibility" frontend/components/map/LegendOverlay.tsx
  </acceptance_criteria>
  <done>Legend overlay button visible on map, clicking it shows/hides a floating panel with active layer legends. Panel is scrollable and positioned above the button in bottom-left of map.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` succeeds with no errors
- Both sidebars have visible toggle buttons
- Legend overlay component exists and is wired into MapView
- NavigationControl renders on map with compass
</verification>

<success_criteria>
- Left sidebar collapses to zero width with smooth transition on toggle
- Right dashboard panel collapses to zero width with smooth transition on toggle
- Toggle buttons remain visible when sidebars are collapsed
- Map has NavigationControl with compass and pitch visualization
- Legend button on map opens floating legend panel
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-interactive-ui/20-01-SUMMARY.md`
</output>
