---
phase: 20-interactive-ui
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - frontend/components/map/WeatherSkybox.tsx
  - frontend/components/map/MapView.tsx
  - backend/app/connectors/bus_interpolation.py
  - backend/app/connectors/gtfs.py
autonomous: true
requirements: [REQ-UI-05, REQ-UI-06]

must_haves:
  truths:
    - "Sky gradient on map reflects current weather conditions"
    - "Blue sky for clear weather, gray for overcast, dark gray for rain"
    - "Bus dots appear on map and move along routes from GTFS schedule"
    - "Bus interpolation connector produces positions when GTFS data available"
  artifacts:
    - path: "frontend/components/map/WeatherSkybox.tsx"
      provides: "Sky layer configuration based on weather data"
      min_lines: 30
    - path: "frontend/components/map/MapView.tsx"
      provides: "WeatherSkybox integration and sky layer"
      contains: "WeatherSkybox"
  key_links:
    - from: "frontend/components/map/WeatherSkybox.tsx"
      to: "frontend/hooks/useKpi.ts"
      via: "reads weather condition from KPI data"
      pattern: "useKpi|weather.*condition"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/WeatherSkybox.tsx"
      via: "renders WeatherSkybox inside Map"
      pattern: "WeatherSkybox"
---

<objective>
Add weather-responsive sky gradient to the map and fix bus position interpolation so buses are visible on the map.

Purpose: The sky gradient provides ambient weather awareness, and fixing bus interpolation completes a key real-time data layer.
Output: Weather skybox component reflecting current conditions, working bus interpolation pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-interactive-ui/20-CONTEXT.md
@frontend/components/map/MapView.tsx
@frontend/hooks/useKpi.ts
@frontend/types/kpi.ts
@frontend/components/map/BusPositionLayer.tsx
@frontend/components/map/BusRouteLayer.tsx
@backend/app/connectors/bus_interpolation.py
@backend/app/connectors/gtfs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weather skybox gradient on map</name>
  <read_first>
    - frontend/components/map/MapView.tsx (Map component, where to add sky layer)
    - frontend/hooks/useKpi.ts (how KPI data is fetched)
    - frontend/types/kpi.ts (WeatherKPI type — condition field)
  </read_first>
  <files>
    frontend/components/map/WeatherSkybox.tsx,
    frontend/components/map/MapView.tsx
  </files>
  <action>
    **Weather Skybox (per REQ-UI-05):**

    MapLibre GL JS supports a `sky` layer type for atmospheric scattering effects. However, the react-map-gl bindings may not expose this directly. Use the map instance to add the sky layer imperatively.

    1. Create `WeatherSkybox.tsx`:
       - Props: `condition: string | null` (weather condition from KPI — values like "clear-day", "clear-night", "cloudy", "fog", "partly-cloudy-day", "partly-cloudy-night", "rain", "sleet", "snow", "wind", etc. from Bright Sky API icon field).
       - Use `useMap()` hook from `react-map-gl/maplibre` to access the map instance.
       - On mount and when condition changes, update the map's fog/atmosphere. MapLibre 5.x supports `map.setFog()` for atmosphere effects.
       - Instead of a sky layer (which requires specific terrain), use `map.setFog()` to set atmospheric gradient:

         For clear/sunny conditions:
         ```
         map.setFog({
           color: '#ddeeff',
           'high-color': '#245bde',
           'horizon-blend': 0.08,
           'space-color': '#1a1a2e',
           'star-intensity': 0.0,
         })
         ```

         For overcast/cloudy:
         ```
         map.setFog({
           color: '#c8c8c8',
           'high-color': '#8a8a8a',
           'horizon-blend': 0.15,
           'space-color': '#4a4a4a',
           'star-intensity': 0.0,
         })
         ```

         For rain:
         ```
         map.setFog({
           color: '#9a9a9a',
           'high-color': '#5a5a5a',
           'horizon-blend': 0.2,
           'space-color': '#2a2a2a',
           'star-intensity': 0.0,
         })
         ```

         For night (clear-night, partly-cloudy-night):
         ```
         map.setFog({
           color: '#1a1a3e',
           'high-color': '#0a0a2e',
           'horizon-blend': 0.1,
           'space-color': '#000011',
           'star-intensity': 0.6,
         })
         ```

       - Map condition strings to fog presets: "clear-day" and "partly-cloudy-day" -> clear, "cloudy" and "fog" -> overcast, "rain" and "sleet" and "snow" -> rain, "clear-night" and "partly-cloudy-night" -> night, "wind" -> clear (wind alone doesn't change sky). Default to clear if condition is null or unknown.
       - Use `useEffect` with condition as dependency to call `map.setFog()`.
       - Return `null` from render (no visible DOM element).

    2. In `MapView.tsx`:
       - Import `WeatherSkybox`.
       - Add `weatherCondition?: string | null` to `MapViewProps`.
       - Render `<WeatherSkybox condition={weatherCondition} />` inside the `<Map>` component.

    3. In `page.tsx` (consumer of MapView):
       - The `useKpi` hook is already called and `data.weather.icon` contains the condition string.
       - Pass `weatherCondition={data?.weather?.icon ?? null}` as a new prop to `<MapView>`.
       - NOTE: `useKpi` is called inside `DashboardPanel`, but weather icon is also available from the KPI response. To avoid prop drilling, call `useKpi(town)` in `page.tsx` too (the hook has its own cache/polling), then pass the condition to MapView.
       - Actually, check if `useKpi` is already called in page.tsx — if not, either add it or find another way. Looking at the code: `useKpi` is NOT called in page.tsx (it is called inside DashboardPanel). Add `const { data: kpiData } = useKpi(town)` in page.tsx and pass `weatherCondition={kpiData?.weather?.icon ?? null}` to MapView.

    **IMPORTANT:** Check MapLibre 5.x docs — the `setFog` API may have changed. If `setFog` is not available, use `map.setStyle()` to add a sky layer to the style spec. Fallback approach: just use `map.setPaintProperty` on a sky layer if one exists.

    **Deferred per CONTEXT.md:** No animated weather particles (rain, snow).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/components/map/WeatherSkybox.tsx
    - grep -q "WeatherSkybox" frontend/components/map/MapView.tsx
    - grep -q "setFog\|sky\|fog" frontend/components/map/WeatherSkybox.tsx
    - grep -q "condition" frontend/components/map/WeatherSkybox.tsx
    - grep -q "weatherCondition" frontend/components/map/MapView.tsx
    - grep -q "useKpi\|weatherCondition" frontend/app/page.tsx
  </acceptance_criteria>
  <done>Sky gradient on map changes based on current weather conditions. Clear sky shows blue gradient, overcast shows gray, rain shows dark gray, night shows dark blue with stars. Updates when weather KPI data refreshes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix bus position interpolation</name>
  <read_first>
    - backend/app/connectors/bus_interpolation.py (full connector code)
    - backend/app/connectors/gtfs.py (GTFS static data loading)
    - backend/app/scheduler.py (connector registration, GTFS URL config)
    - frontend/components/map/BusPositionLayer.tsx (frontend rendering)
    - frontend/components/map/BusRouteLayer.tsx (route rendering)
  </read_first>
  <files>
    backend/app/connectors/bus_interpolation.py,
    backend/app/connectors/gtfs.py
  </files>
  <action>
    **Fix Bus Interpolation (per REQ-UI-06):**

    The BusInterpolationConnector exists and the algorithms are correct (tested). The issue per CONTEXT.md is:
    1. GTFS static data needs to be loaded first (GTFSConnector must have run successfully)
    2. GTFS download URL (NVBW) may return 404
    3. Fallback: use schedule-only interpolation if no GTFS-RT delays available

    **Diagnosis steps (execute in order):**

    1. Read `backend/app/connectors/gtfs.py` fully to find the GTFS URL and how it's configured.
    2. Check the connector config table in the database to see if BusInterpolationConnector has a `gtfs_url` configured. Run:
       ```bash
       cd /Users/hannessuhr/Projects/city-data-platform && docker compose exec postgres psql -U cdp -d cdp -c "SELECT name, config FROM connector_configs WHERE name LIKE '%Bus%' OR name LIKE '%GTFS%' OR name LIKE '%gtfs%';"
       ```
    3. If no config exists, the connector will skip (`gtfs_url not configured`). Create a seed/migration or use the admin API to add the config.

    **Fixes to apply:**

    A. **GTFS URL fallback:** In `bus_interpolation.py`, update `_download_cached_gtfs` to try multiple GTFS URLs:
       - Primary: the configured `gtfs_url` (e.g., NVBW bwgesamt URL)
       - Fallback URLs to try if primary returns 404:
         - `https://www.nvbw.de/fileadmin/nvbw/open-data/fahrplandaten_mit_liniennetz/bwgesamt.zip`
         - `https://download.data.public.lu/resources/gtfs/20240101-000000/gtfs.zip` (this is wrong, just use NVBW)
       - Add the fallback logic in `_download_cached_gtfs`: if the primary URL returns 404 or fails, try `gtfs_url_fallback` from config if available.
       - Log clearly which URL succeeded.

    B. **Schedule-only interpolation already works:** The connector already defaults `delay_seconds=0` when no RT data is found (line 314: `delays.get(trip.trip_id, 0)`). This is correct. No change needed for fallback behavior.

    C. **Ensure GTFS URL is configured:** Add a check in the connector's `run()` method. If `gtfs_url` is empty, try a reasonable default URL:
       ```python
       gtfs_url = self.config.config.get("gtfs_url", "")
       if not gtfs_url:
           # Default to NVBW Baden-Wuerttemberg GTFS for OstalbMobil coverage
           gtfs_url = "https://www.nvbw.de/fileadmin/nvbw/open-data/fahrplandaten_mit_liniennetz/bwgesamt.zip"
           logger.info("BusInterpolationConnector: using default NVBW GTFS URL")
       ```

    D. **Improve error resilience in GTFS download:**
       - Add `follow_redirects=True` to the httpx client in `_download_cached_gtfs`.
       - Add User-Agent header: `"CityDataPlatform/2.0"` (some servers block default httpx UA).
       - If ZIP is unexpectedly small (< 1000 bytes), log warning and treat as failure (might be an HTML error page).

    E. **In `gtfs.py`:** Apply the same `follow_redirects=True` and User-Agent header fix to the GTFSConnector's fetch method for consistency.

    **Do NOT change:**
    - The shape_walk or interpolate_position algorithms (they are correct and tested).
    - The frontend BusPositionLayer or BusRouteLayer (they work correctly when data is available).
    - The scheduler registration (it already registers BusInterpolationConnector).
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && python -m pytest tests/connectors/test_bus_interpolation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "follow_redirects" backend/app/connectors/bus_interpolation.py
    - grep -q "User-Agent" backend/app/connectors/bus_interpolation.py
    - grep -q "bwgesamt" backend/app/connectors/bus_interpolation.py
    - grep -q "follow_redirects" backend/app/connectors/gtfs.py
  </acceptance_criteria>
  <done>Bus interpolation connector has GTFS URL fallback, improved HTTP resilience (follow_redirects, User-Agent), and works with schedule-only interpolation when no GTFS-RT delays are available. Existing tests still pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` succeeds
- `cd backend && python -m pytest tests/connectors/test_bus_interpolation.py -x` passes
- WeatherSkybox component exists and is wired into MapView
- Bus interpolation connector has improved URL handling
</verification>

<success_criteria>
- Sky gradient on map changes with weather conditions
- Bus interpolation connector can download GTFS data with fallback URL
- Bus interpolation connector handles HTTP redirects and blocked user agents
- Existing bus interpolation unit tests still pass
- Frontend build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/20-interactive-ui/20-03-SUMMARY.md`
</output>
