---
phase: 19-feature-registry
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/components/map/UnifiedBuildingPopup.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/map/BuildingsLayer.tsx
  - frontend/hooks/useFeatureData.ts
autonomous: false
requirements: [REQ-REGISTRY-05, REQ-REGISTRY-06]

must_haves:
  truths:
    - "Buildings are clickable in both 2D footprint and 3D extruded views"
    - "Clicking a building opens a unified data card showing all attached layers"
    - "Buildings without data show 'Keine Daten verfuegbar' message"
    - "Existing layer popups continue working for non-building clicks"
  artifacts:
    - path: "frontend/components/map/UnifiedBuildingPopup.tsx"
      provides: "Unified building data card with multi-domain sections"
      min_lines: 80
    - path: "frontend/hooks/useFeatureData.ts"
      provides: "Hook to fetch /api/features/{id}/data"
      contains: "useFeatureData"
    - path: "frontend/components/map/MapView.tsx"
      provides: "Updated click handler routing building clicks to UnifiedBuildingPopup"
      contains: "buildings-3d"
    - path: "frontend/components/map/BuildingsLayer.tsx"
      provides: "Interactive buildings layer"
      contains: "interactive"
  key_links:
    - from: "frontend/hooks/useFeatureData.ts"
      to: "/api/features/{feature_id}/data"
      via: "fetch call"
      pattern: "api/features"
    - from: "frontend/components/map/MapView.tsx"
      to: "frontend/components/map/UnifiedBuildingPopup.tsx"
      via: "popup rendering for building domain"
      pattern: "UnifiedBuildingPopup"
    - from: "frontend/components/map/MapView.tsx"
      to: "buildings-3d"
      via: "interactiveLayerIds includes building layer"
      pattern: "buildings-3d"
---

<objective>
Make every building clickable in 2D and 3D views, opening a unified data card (UnifiedBuildingPopup) that shows all attached data layers from the feature registry API.

Purpose: This is the core user-facing feature -- click any building to see its full data profile (heat demand, solar, Fernwaerme, demographics, energy class).
Output: UnifiedBuildingPopup component, useFeatureData hook, updated MapView and BuildingsLayer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-feature-registry/19-CONTEXT.md
@.planning/phases/19-feature-registry/19-01-SUMMARY.md

@frontend/components/map/MapView.tsx
@frontend/components/map/BuildingsLayer.tsx
@frontend/components/map/HeatDemandPopup.tsx
@frontend/components/map/SolarPopup.tsx

<interfaces>
<!-- API from Plan 01: -->
<!-- GET /api/features/{feature_id}/data -->
<!-- Response: { feature_id, semantic_id, domain, properties, geometry, observations: { domain: { timestamp, values } } } -->

From frontend/components/map/MapView.tsx:
```typescript
interface PopupInfo {
  longitude: number;
  latitude: number;
  feature: GeoJSON.Feature;
  domain: 'transit' | 'airQuality' | 'water' | 'traffic' | 'trafficFlow' | ... | 'building';
}
// interactiveLayerIds array controls which layers respond to clicks
// onClick handler maps layerId to domain string
```

From frontend/components/map/BuildingsLayer.tsx:
```typescript
// Currently uses source "openmaptiles" with source-layer "building"
// Layer id: "buildings-3d"
// No interactivity (not in interactiveLayerIds)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useFeatureData hook + UnifiedBuildingPopup component</name>
  <files>frontend/hooks/useFeatureData.ts, frontend/components/map/UnifiedBuildingPopup.tsx</files>
  <read_first>
    - frontend/hooks/useLayerData.ts (pattern for data fetching hooks)
    - frontend/components/map/HeatDemandPopup.tsx (popup pattern for buildings)
    - frontend/components/map/SolarPopup.tsx (solar data display pattern)
    - frontend/components/map/EnergyPopup.tsx (energy data display pattern)
    - frontend/components/map/DataSourceSection.tsx (data source section component)
    - frontend/lib/layer-metadata.ts (LAYER_METADATA registry for DataSourceSection)
  </read_first>
  <action>
    1. Create frontend/hooks/useFeatureData.ts:
       - Export function useFeatureData(featureId: string | null)
       - Returns { data, loading, error }
       - Fetches from /api/features/{featureId}/data when featureId is non-null
       - Uses useState + useEffect pattern (no SWR/React Query -- match existing hooks)
       - Clears data when featureId changes
       - Type for response:
         ```typescript
         interface FeatureData {
           feature_id: string;
           semantic_id: string | null;
           domain: string;
           properties: Record<string, unknown>;
           observations: Record<string, { timestamp: string; values: Record<string, unknown> }>;
         }
         ```

    2. Create frontend/components/map/UnifiedBuildingPopup.tsx:
       - Props: { feature: GeoJSON.Feature; longitude: number; latitude: number }
       - On mount: extract feature_id from feature.properties (try feature_id, then id)
       - If no feature_id available: attempt lookup by building properties from vector tile (render "Gebaeude" header with available tile properties only, no API call)
       - If feature_id available: use useFeatureData(featureId) to fetch all attached data
       - Loading state: show skeleton/spinner
       - Render sections (show only if data exists for that domain):
         - Header: "Gebaeude" with address/name from properties
         - Waerme section: heat_demand_kwh from observations.energy or properties
         - Solar section: solar_installed_kw, solar_production from observations.energy
         - Fernwaerme section: "Angeschlossen" / "Nicht angeschlossen" from properties
         - Demografie section: from observations.demographics
         - Energie section: energy class if available
       - If no observations at all: show "Keine Daten verfuegbar" message
       - Use existing UI components (Badge from shadcn, DataSourceSection)
       - German labels throughout (Waerme, Solar, Fernwaerme, Demografie)
       - Max width 320px matching other popups
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useFeatureData" frontend/hooks/useFeatureData.ts
    - grep -q "api/features" frontend/hooks/useFeatureData.ts
    - grep -q "UnifiedBuildingPopup" frontend/components/map/UnifiedBuildingPopup.tsx
    - grep -q "Keine Daten" frontend/components/map/UnifiedBuildingPopup.tsx
    - grep -q "observations" frontend/components/map/UnifiedBuildingPopup.tsx
  </acceptance_criteria>
  <done>
    - useFeatureData hook fetches feature data from API and returns typed response
    - UnifiedBuildingPopup renders all attached domain data in labeled sections
    - German labels used throughout
    - Empty state handled with "Keine Daten verfuegbar"
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire buildings into MapView click handling</name>
  <files>frontend/components/map/MapView.tsx, frontend/components/map/BuildingsLayer.tsx</files>
  <read_first>
    - frontend/components/map/MapView.tsx (full file -- click handler, interactiveLayerIds, popup routing)
    - frontend/components/map/BuildingsLayer.tsx (current layer config)
  </read_first>
  <action>
    1. Update BuildingsLayer.tsx:
       - The buildings-3d layer uses source "openmaptiles" vector tiles
       - MapLibre vector tile features ARE queryable when added to interactiveLayerIds
       - No changes needed to BuildingsLayer itself -- interactivity is controlled by MapView's interactiveLayerIds

    2. Update MapView.tsx:
       - Add 'buildings-3d' to the interactiveLayerIds array
       - Add 'building' to the PopupInfo domain union type
       - In onClick handler: add condition for layerId === 'buildings-3d' -> domain = 'building'
       - Import UnifiedBuildingPopup
       - In popup rendering section: add case for popupInfo.domain === 'building':
         ```tsx
         popupInfo.domain === 'building' ? (
           <UnifiedBuildingPopup
             feature={popupInfo.feature}
             longitude={popupInfo.longitude}
             latitude={popupInfo.latitude}
           />
         ) :
         ```
       - Place this BEFORE the final fallback FeaturePopup case
       - Ensure existing popup routing for all other domains is unchanged
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "buildings-3d" frontend/components/map/MapView.tsx
    - grep -q "building" frontend/components/map/MapView.tsx
    - grep -q "UnifiedBuildingPopup" frontend/components/map/MapView.tsx
  </acceptance_criteria>
  <done>
    - buildings-3d layer ID in interactiveLayerIds
    - Clicking a building in 2D or 3D opens UnifiedBuildingPopup
    - All existing popup routing unchanged for other layers
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify building click and popup</name>
  <files>frontend/components/map/MapView.tsx</files>
  <action>
    Human verification of building click and popup behavior. No code changes -- verify the work from Tasks 1 and 2.

    Steps:
    1. Start the app: cd frontend and npm run dev (port 4000)
    2. Navigate to the map view of Aalen
    3. Enable 3D buildings toggle in sidebar
    4. Click on a building -- should see UnifiedBuildingPopup appear
    5. Check that building info sections render (may show "Keine Daten verfuegbar" if no data attached yet)
    6. Switch to 2D view (disable 3D) -- click a building footprint, same popup should appear
    7. Click on a non-building feature (transit stop, sensor) -- should show original domain popup, not building popup
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms buildings are clickable in 2D and 3D, popup renders correctly, existing popups unaffected</done>
</task>

</tasks>

<verification>
- Buildings respond to click events in both 2D and 3D modes
- UnifiedBuildingPopup renders with correct German section labels
- API call to /api/features/{id}/data succeeds and data displays
- Non-building layer clicks still show domain-specific popups
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Every building on the map is clickable in both 2D footprint and 3D extruded views
- Click opens UnifiedBuildingPopup showing all available data (heat, solar, Fernwaerme, demographics)
- Buildings without attached data show "Keine Daten verfuegbar"
- Existing popups for all other layers continue working
</success_criteria>

<output>
After completion, create `.planning/phases/19-feature-registry/19-02-SUMMARY.md`
</output>
