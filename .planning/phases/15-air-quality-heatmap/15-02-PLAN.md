---
phase: 15-air-quality-heatmap
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/components/map/AirQualityHeatmapLayer.tsx
  - frontend/components/map/AQILayer.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/sidebar/LayerToggle.tsx
  - frontend/app/page.tsx
  - frontend/lib/api.ts
autonomous: true
requirements: [REQ-AIR-02, REQ-AIR-03, REQ-AIR-04, REQ-AIR-05]

must_haves:
  truths:
    - "Translucent color overlay renders on map with green->yellow->orange->red->purple ramp"
    - "User can toggle between PM2.5 / PM10 / NO2 / O3 pollutants"
    - "Sensor points show as pulsing dots with live readings"
    - "Clicking a sensor opens a time-series popup chart"
  artifacts:
    - path: "frontend/components/map/AirQualityHeatmapLayer.tsx"
      provides: "IDW grid heatmap overlay with pollutant toggle"
      contains: "AirQualityHeatmapLayer"
    - path: "frontend/components/map/AQILayer.tsx"
      provides: "Updated sensor points with pulsing animation"
      contains: "pulsing"
  key_links:
    - from: "frontend/components/map/AirQualityHeatmapLayer.tsx"
      to: "/api/layers/air_quality?feature_type=air_grid"
      via: "fetch with feature_type param"
      pattern: "feature_type.*air_grid"
    - from: "frontend/components/map/MapView.tsx"
      to: "AirQualityHeatmapLayer"
      via: "component import and rendering"
      pattern: "AirQualityHeatmapLayer"
    - from: "frontend/app/page.tsx"
      to: "pollutant state"
      via: "useState for active pollutant"
      pattern: "pollutant|activePollutant"
---

<objective>
Build the frontend air quality heatmap layer: translucent color grid overlay driven by IDW-interpolated values from Plan 01, pollutant toggle (PM2.5/PM10/NO2/O3), pulsing sensor dots with live readings, and time-series popup chart on sensor click.

Purpose: Visual continuous air quality surface for citizens (REQ-AIR-02 through REQ-AIR-05)
Output: Working heatmap overlay with pollutant switching and interactive sensor popups
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-air-quality-heatmap/15-01-SUMMARY.md
@frontend/components/map/AQILayer.tsx
@frontend/components/map/MapView.tsx
@frontend/components/map/TrafficFlowLayer.tsx
@frontend/components/sidebar/LayerToggle.tsx
@frontend/hooks/useLayerData.ts
@frontend/app/page.tsx
@frontend/lib/api.ts
@frontend/components/dashboard/TimeSeriesChart.tsx

<interfaces>
<!-- Existing patterns from codebase -->

From frontend/hooks/useLayerData.ts:
```typescript
export function useLayerData(domain: string, town?: string, timestamp?: Date | null): {
  data: LayerResponse | null;
  lastFetched: Date | null;
  error: boolean;
}
```

From frontend/types/geojson.ts (LayerResponse):
```typescript
// GeoJSON FeatureCollection with attribution
interface LayerResponse { features: Feature[]; attribution: Attribution[]; ... }
```

From frontend/lib/api.ts:
```typescript
export async function fetchLayer(domain: string, town: string, timestamp?: Date | null): Promise<LayerResponse>
```

From Plan 01 backend API:
- GET /api/layers/air_quality?town=aalen&feature_type=air_grid
  Returns GeoJSON with grid cell features, each having properties:
  { feature_type: "air_grid", pm25: number|null, pm10: number|null, no2: number|null, o3: number|null }

- GET /api/layers/air_quality?town=aalen (or feature_type=sensor)
  Returns existing sensor point features (unchanged)

From frontend/components/map/AQILayer.tsx:
```typescript
interface AQILayerProps { data: LayerResponse | null; visible: boolean; }
// Source id: "air-quality", layers: aqi-heatmap, aqi-circles, aqi-points
```

From frontend/components/dashboard/TimeSeriesChart.tsx:
```typescript
interface TimeSeriesChartProps {
  domain: 'air_quality' | 'weather';
  points: TimeseriesPoint[];
  loading: boolean; error: boolean;
  dateRange: { from: Date; to: Date };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AirQualityHeatmapLayer with pollutant toggle and pulsing sensors</name>
  <files>
    frontend/components/map/AirQualityHeatmapLayer.tsx
    frontend/components/map/AQILayer.tsx
    frontend/lib/api.ts
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/map/AQILayer.tsx
    frontend/components/map/MapView.tsx
    frontend/components/map/TrafficFlowLayer.tsx
    frontend/hooks/useLayerData.ts
    frontend/lib/api.ts
    frontend/app/page.tsx
    frontend/components/sidebar/LayerToggle.tsx
  </read_first>
  <action>
    1. In frontend/lib/api.ts, add a `fetchGridLayer` function (or extend `fetchLayer` with optional `featureType` param) that calls `/api/layers/air_quality?town={town}&feature_type=air_grid`. The grid data needs separate fetching from sensor points since they use different source IDs on the map.

    2. Create frontend/components/map/AirQualityHeatmapLayer.tsx:
       - Props: { data: LayerResponse | null; visible: boolean; activePollutant: 'pm25' | 'pm10' | 'no2' | 'o3' }
       - Render grid cells as a GeoJSON fill layer using MapLibre's `fill` type
       - Each grid cell is a Point in the data -- render as small squares using `circle` layer with large radius, OR better: use a `fill` layer with the grid cells converted to small polygons client-side, OR simplest: use MapLibre `circle` layer with radius ~15-20px that creates a continuous-looking surface at typical zoom levels
       - RECOMMENDED approach: Use MapLibre `heatmap` layer type on the grid points, with heatmap-weight driven by the active pollutant value. This creates the smooth translucent overlay naturally.
       - Color ramp (REQ-AIR-02): green (#00e400) -> yellow (#ffff00) -> orange (#ff7e00) -> red (#ff0000) -> purple (#8f3f97)
       - Map pollutant value to weight: normalize by WHO guideline thresholds:
         PM2.5: 0-15 good, 15-30 moderate, 30-55 unhealthy, 55+ very unhealthy
         PM10: 0-45 good, 45-90 moderate, 90-180 unhealthy, 180+ very unhealthy
         NO2: 0-40 good, 40-100 moderate, 100-200 unhealthy, 200+ very unhealthy
         O3: 0-60 good, 60-120 moderate, 120-180 unhealthy, 180+ very unhealthy
       - Use `['get', activePollutant]` for the property access -- but since MapLibre paint expressions are static, use a key-based approach: when activePollutant changes, rebuild the layer spec with the right property name
       - Source ID: "air-quality-grid" (separate from existing "air-quality" source)
       - Heatmap opacity: 0.5 (translucent per REQ-AIR-02)

    3. Update frontend/components/map/AQILayer.tsx to add pulsing animation to sensor dots (REQ-AIR-04):
       - Add CSS animation via a `@keyframes pulse` that scales circle-radius between 6 and 10
       - MapLibre doesn't support CSS keyframes on layers directly, so use `circle-radius` with a time-based expression, OR simpler: add `circle-blur: 0.4` and `circle-stroke-width: 2` with `circle-stroke-opacity` to create a "glow" effect that suggests pulsing
       - Alternative (better): Use the existing circle layer but add a second circle layer behind it with larger radius and lower opacity for the pulse effect. Use `circle-radius: ['interpolate', ['linear'], ['zoom'], 10, 12, 14, 20]` and `circle-opacity: 0.3` for the pulse ring

    4. In frontend/app/page.tsx:
       - Add state: `const [activePollutant, setActivePollutant] = useState<'pm25' | 'pm10' | 'no2' | 'o3'>('pm25')`
       - Add a separate data fetch for grid layer: use fetchGridLayer or a new useLayerData call with feature_type param
       - Pass activePollutant and grid data down to MapView
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AirQualityHeatmapLayer" frontend/components/map/AirQualityHeatmapLayer.tsx
    - grep -q "activePollutant" frontend/components/map/AirQualityHeatmapLayer.tsx
    - grep -q "air-quality-grid" frontend/components/map/AirQualityHeatmapLayer.tsx
    - grep -q "pm25.*pm10.*no2.*o3" frontend/components/map/AirQualityHeatmapLayer.tsx
    - grep -q "activePollutant" frontend/app/page.tsx
    - grep -q "air_grid\|feature_type" frontend/lib/api.ts
  </acceptance_criteria>
  <done>Heatmap overlay renders grid cells with color ramp, pollutant toggle state wired from page, sensor dots have pulsing glow effect</done>
</task>

<task type="auto">
  <name>Task 2: MapView wiring, pollutant toggle UI, and sensor popup with time-series chart</name>
  <files>
    frontend/components/map/MapView.tsx
    frontend/components/sidebar/LayerToggle.tsx
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/map/MapView.tsx
    frontend/components/sidebar/LayerToggle.tsx
    frontend/app/page.tsx
    frontend/components/dashboard/TimeSeriesChart.tsx
    frontend/components/sidebar/AQILegend.tsx
  </read_first>
  <action>
    1. In frontend/components/map/MapView.tsx:
       - Import AirQualityHeatmapLayer
       - Add props: airQualityGridData (LayerResponse | null), activePollutant ('pm25'|'pm10'|'no2'|'o3')
       - Render <AirQualityHeatmapLayer> alongside existing <AQILayer>, controlled by same airQuality visibility toggle
       - For sensor click popup (REQ-AIR-05): when a click hits an aqi-points layer feature, show a popup containing a small TimeSeriesChart. Use the existing pattern from MapView's popup handling -- detect click on 'aqi-points' or 'aqi-circles' layer, set popupInfo with domain='airQuality'. The existing popup rendering already handles airQuality domain -- extend it to include an inline time-series chart fetched from /api/timeseries/air_quality filtered to that feature_id.
       - Add a small inline component `SensorPopupChart` that takes feature_id, fetches 24h of timeseries data, and renders a compact Recharts line chart showing pm25/pm10 over time. Use the existing useTimeseries hook pattern but filtered to one feature.

    2. In frontend/components/sidebar/LayerToggle.tsx:
       - Add pollutant toggle controls within or near the air quality layer toggle (REQ-AIR-03)
       - When airQuality layer is active, show a row of 4 small toggle buttons: PM2.5 | PM10 | NO2 | O3
       - Active pollutant highlighted with primary color, others muted
       - Props: activePollutant, onPollutantChange callback
       - Style: small pill buttons below the AQI toggle, using existing Tailwind patterns

    3. In frontend/app/page.tsx:
       - Pass activePollutant and setActivePollutant to Sidebar/LayerToggle
       - Pass activePollutant and gridData to MapView
       - Wire the pollutant toggle callback through to LayerToggle
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AirQualityHeatmapLayer" frontend/components/map/MapView.tsx
    - grep -q "airQualityGridData" frontend/components/map/MapView.tsx
    - grep -q "activePollutant" frontend/components/map/MapView.tsx
    - grep -q "activePollutant\|onPollutantChange" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "PM2.5\|PM10\|NO2\|O3" frontend/components/sidebar/LayerToggle.tsx
    - grep -q "activePollutant" frontend/app/page.tsx
  </acceptance_criteria>
  <done>MapView renders heatmap grid overlay alongside sensor points, sidebar shows pollutant toggle (PM2.5/PM10/NO2/O3), clicking sensor dot opens popup with time-series chart</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no type errors
- AirQualityHeatmapLayer component exists with activePollutant prop
- Pollutant toggle renders PM2.5/PM10/NO2/O3 options in sidebar
- MapView renders both AQILayer (sensor points) and AirQualityHeatmapLayer (grid) together
- Sensor click popup includes time-series chart component
</verification>

<success_criteria>
- Translucent color heatmap overlay renders on map with green->yellow->orange->red->purple ramp (REQ-AIR-02)
- Pollutant toggle switches display between PM2.5/PM10/NO2/O3 (REQ-AIR-03)
- Sensor points display as dots with pulsing glow effect and live readings (REQ-AIR-04)
- Clicking a sensor opens popup with time-series chart (REQ-AIR-05)
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-air-quality-heatmap/15-02-SUMMARY.md`
</output>
