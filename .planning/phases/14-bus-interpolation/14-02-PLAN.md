---
phase: 14-bus-interpolation
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/components/map/BusPositionLayer.tsx
  - frontend/components/map/BusPopup.tsx
  - frontend/components/map/BusRouteLayer.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [REQ-BUS-02, REQ-BUS-03, REQ-BUS-04, REQ-BUS-06]

must_haves:
  truths:
    - "Bus icons visible on map as colored dots moving along routes"
    - "Bus icons colored by delay: green <2min, yellow 2-5min, orange 5-10min, red >10min"
    - "Click on bus shows popup with line number, destination, delay, next stop"
    - "Faint route lines visible when bus layer is active"
    - "Bus data refreshes every 30 seconds with smooth visual transitions"
  artifacts:
    - path: "frontend/components/map/BusPositionLayer.tsx"
      provides: "Bus position circles with delay-based coloring"
      min_lines: 30
    - path: "frontend/components/map/BusPopup.tsx"
      provides: "Bus click popup with trip details"
      min_lines: 20
    - path: "frontend/components/map/BusRouteLayer.tsx"
      provides: "Faint route polylines for active bus routes"
      min_lines: 20
  key_links:
    - from: "frontend/components/map/BusPositionLayer.tsx"
      to: "/api/layers/transit"
      via: "useLayerData hook filtering feature_type=bus_position"
      pattern: "feature_type.*bus_position"
    - from: "frontend/components/map/MapView.tsx"
      to: "BusPositionLayer"
      via: "conditional render when busPositionVisible"
      pattern: "BusPositionLayer"
    - from: "frontend/app/page.tsx"
      to: "busPosition layer state"
      via: "useUrlState layers includes bus_position"
      pattern: "bus_position"
---

<objective>
Build the frontend bus position visualization: colored moving dots on map, faint route lines, click popup with trip details, and sidebar toggle. Data refreshes every 30 seconds.

Purpose: REQ-BUS-02 (colored bus icons), REQ-BUS-03 (popup), REQ-BUS-04 (route lines), REQ-BUS-06 (30s animation).
Output: BusPositionLayer, BusRouteLayer, BusPopup components wired into MapView, Sidebar, and page state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bus-interpolation/14-CONTEXT.md
@.planning/phases/14-bus-interpolation/14-01-SUMMARY.md

@frontend/components/map/MapView.tsx
@frontend/components/map/ParkingLayer.tsx
@frontend/components/map/ParkingPopup.tsx
@frontend/components/map/TrafficFlowLayer.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/app/page.tsx
@frontend/hooks/useLayerData.ts
@frontend/lib/api.ts

<interfaces>
<!-- Bus position features served by /api/layers/transit (from Plan 01) -->
<!-- Each bus position is a GeoJSON Feature with these properties: -->
```typescript
// Feature properties for feature_type="bus_position" in transit layer
interface BusPositionProperties {
  feature_type: "bus_position";
  trip_id: string;
  route_id: string;
  line_name: string;       // e.g. "71", "82"
  destination: string;     // e.g. "Wasseralfingen Bf"
  next_stop: string;       // e.g. "Rathaus"
  delay_seconds: number;   // 0 = on time, positive = late
  bearing: number;         // degrees 0-360
  progress: number;        // 0.0-1.0 along route
  departed: boolean;       // false if bus hasn't left first stop yet
}
```

From frontend/hooks/useLayerData.ts:
```typescript
// Polls every 60s by default. For bus layer, we need 30s refresh.
// Either create a dedicated hook or pass refresh interval to useLayerData.
export function useLayerData(domain: string, town?: string, timestamp?: Date | null): {
  data: LayerResponse | null;
  lastFetched: Date | null;
  error: boolean;
}
```

From frontend/components/map/MapView.tsx:
```typescript
// PopupInfo domain union type needs 'busPosition' added
// interactiveLayerIds needs 'bus-position-points' added
// MapViewProps needs busPositionVisible prop
```

From frontend/components/sidebar/Sidebar.tsx:
```typescript
// layerVisibility needs busPosition field
// onToggleLayer union needs 'busPosition' added
```

From frontend/app/page.tsx:
```typescript
// LAYER_KEYS needs busPosition: 'bus_position'
// layerVisibility needs busPosition field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BusPositionLayer, BusRouteLayer, and BusPopup components</name>
  <files>
    frontend/components/map/BusPositionLayer.tsx
    frontend/components/map/BusRouteLayer.tsx
    frontend/components/map/BusPopup.tsx
  </files>
  <read_first>
    frontend/components/map/ParkingLayer.tsx
    frontend/components/map/ParkingPopup.tsx
    frontend/components/map/TrafficFlowLayer.tsx
    frontend/components/map/TransitLayer.tsx
    frontend/hooks/useLayerData.ts
    frontend/types/geojson.ts
  </read_first>
  <action>
    IMPORTANT: Read node_modules/next/dist/docs/ for any Next.js 16 breaking changes before writing components.

    Create frontend/components/map/BusPositionLayer.tsx:
    - Props: { town: string; visible: boolean }
    - Use useLayerData('transit', town) to get transit features, then filter to feature_type="bus_position"
    - Use a SEPARATE useEffect with 30s setInterval to re-fetch transit data (override the default 60s in useLayerData). Alternative: create inline fetch with 30s refresh interval using useState + useEffect + fetchLayer directly from @/lib/api, since useLayerData has hardcoded 60s. Per REQ-BUS-06 the bus layer needs 30s updates.
    - MapLibre circle layer "bus-position-points" with:
      - circle-color: step expression on delay_seconds:
        green (#22c55e) for delay < 120 (2min)
        yellow (#eab308) for 120-300 (2-5min)
        orange (#f97316) for 300-600 (5-10min)
        red (#ef4444) for >= 600 (10min+)
      - circle-radius: 7 (smaller than parking dots)
      - circle-stroke-width: 2, circle-stroke-color: white
    - Use Source id="bus-positions" separate from existing "transit" Source to avoid interference
    - Add a symbol layer "bus-line-labels" showing line_name below each dot (text-offset [0, 1.5], text-size 10)
    - For smooth animation between 30s updates per REQ-BUS-06: use MapLibre's built-in GeoJSON source transition. The GeoJSON source automatically interpolates point positions when data changes if the feature IDs match. Set generateId: false and use trip_id as feature id in the GeoJSON to enable this. The 30s update with matching IDs will produce visual movement.

    Create frontend/components/map/BusRouteLayer.tsx:
    - Props: { town: string; visible: boolean }
    - Use useLayerData('transit', town) to get transit features
    - Filter to feature_type="shape" (existing GTFS shapes from GTFSConnector)
    - Only show shapes whose shape_id corresponds to an active bus route (match via route_id in bus_position features)
    - If matching is complex, simpler approach: show ALL transit shapes as faint lines when bus layer is active (shapes are already bbox-filtered to Aalen)
    - MapLibre line layer with line-color: "#6366f1" (indigo), line-opacity: 0.25, line-width: 2
    - Source id="bus-routes" separate from existing transit Source

    Create frontend/components/map/BusPopup.tsx:
    - Props: { feature: Feature }
    - Display per REQ-BUS-03:
      - Line number: bold "Linie {line_name}" (e.g. "Linie 71")
      - Destination: "Richtung {destination}"
      - Delay: colored text, e.g. "+3 Min" in yellow, "puenktlich" in green if delay < 60s
      - Next stop: "Naechster Halt: {next_stop}"
    - Delay color matches the circle color thresholds
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && test -f frontend/components/map/BusPositionLayer.tsx && test -f frontend/components/map/BusRouteLayer.tsx && test -f frontend/components/map/BusPopup.tsx && grep -q "bus-position-points" frontend/components/map/BusPositionLayer.tsx && grep -q "delay_seconds" frontend/components/map/BusPopup.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "bus-position-points" frontend/components/map/BusPositionLayer.tsx
    - grep -q "delay_seconds" frontend/components/map/BusPositionLayer.tsx
    - grep -q "#22c55e" frontend/components/map/BusPositionLayer.tsx
    - grep -q "line_name" frontend/components/map/BusPopup.tsx
    - grep -q "destination" frontend/components/map/BusPopup.tsx
    - grep -q "next_stop" frontend/components/map/BusPopup.tsx
    - grep -q "bus-routes" frontend/components/map/BusRouteLayer.tsx
    - grep -q "line-opacity" frontend/components/map/BusRouteLayer.tsx
  </acceptance_criteria>
  <done>
    BusPositionLayer renders colored circle dots for each active bus filtered from transit layer data, with delay-based step color expression. BusRouteLayer shows faint route lines. BusPopup displays line number, destination, delay, next stop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire bus components into MapView, Sidebar, and page state</name>
  <files>
    frontend/components/map/MapView.tsx
    frontend/components/sidebar/Sidebar.tsx
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/map/MapView.tsx
    frontend/components/sidebar/Sidebar.tsx
    frontend/app/page.tsx
    frontend/components/map/BusPositionLayer.tsx
    frontend/components/map/BusRouteLayer.tsx
    frontend/components/map/BusPopup.tsx
  </read_first>
  <action>
    IMPORTANT: Read node_modules/next/dist/docs/ for any Next.js 16 breaking changes before modifying files.

    1. frontend/app/page.tsx:
       - Add to layerVisibility: busPosition: state.layers.includes('bus_position')
       - Add to LAYER_KEYS: busPosition: 'bus_position'
       - Pass busPositionVisible={layerVisibility.busPosition} to MapView

    2. frontend/components/sidebar/Sidebar.tsx:
       - Add 'busPosition' to layerVisibility interface (busPosition?: boolean)
       - Add 'busPosition' to onToggleLayer union type
       - Add toggle in the Ebenen section (near the OEPNV toggle):
         <LayerToggle id="bus-position-toggle" label="Bus-Positionen (live)" checked={layerVisibility.busPosition ?? false} onCheckedChange={() => onToggleLayer('busPosition')} />

    3. frontend/components/map/MapView.tsx:
       - Import BusPositionLayer, BusRouteLayer, BusPopup
       - Add busPositionVisible prop to MapViewProps (default false)
       - Add 'busPosition' to PopupInfo domain union type
       - Add 'bus-position-points' to interactiveLayerIds array
       - Add click handler routing: layerId === 'bus-position-points' -> domain = 'busPosition'
       - Render BusPositionLayer and BusRouteLayer conditionally:
         {busPositionVisible && (
           <>
             <BusRouteLayer town={town} visible={true} />
             <BusPositionLayer town={town} visible={true} />
           </>
         )}
       - Add popup routing: busPosition domain -> <BusPopup feature={popupInfo.feature} />
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && grep -q "busPositionVisible" frontend/components/map/MapView.tsx && grep -q "bus_position" frontend/app/page.tsx && grep -q "busPosition" frontend/components/sidebar/Sidebar.tsx && grep -q "BusPopup" frontend/components/map/MapView.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "BusPositionLayer" frontend/components/map/MapView.tsx
    - grep -q "BusRouteLayer" frontend/components/map/MapView.tsx
    - grep -q "BusPopup" frontend/components/map/MapView.tsx
    - grep -q "bus-position-points" frontend/components/map/MapView.tsx
    - grep -q "busPosition" frontend/components/sidebar/Sidebar.tsx
    - grep -q "Bus-Positionen" frontend/components/sidebar/Sidebar.tsx
    - grep -q "bus_position" frontend/app/page.tsx
    - grep -q "busPosition" frontend/app/page.tsx
  </acceptance_criteria>
  <done>
    Bus position layer fully wired: sidebar toggle controls visibility via URL state, MapView renders BusPositionLayer + BusRouteLayer when active, click on bus dot opens BusPopup with trip details. Layer key "bus_position" persists in URL params.
  </done>
</task>

</tasks>

<verification>
- Frontend builds without errors: `cd frontend && pnpm build`
- BusPositionLayer filters transit data to feature_type="bus_position"
- BusPopup renders line_name, destination, delay, next_stop
- BusRouteLayer shows faint route shapes when bus layer active
- Sidebar has "Bus-Positionen (live)" toggle
- URL param "bus_position" toggles bus layer visibility
- 30s data refresh interval for bus positions
</verification>

<success_criteria>
- Bus icons visible on map as colored dots (green/yellow/orange/red by delay)
- Click popup shows "Linie X", "Richtung Y", delay, next stop
- Faint route lines visible when bus layer active
- Sidebar toggle "Bus-Positionen (live)" controls layer
- Data refreshes every 30 seconds
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/14-bus-interpolation/14-02-SUMMARY.md`
</output>
