---
phase: 12-kocher-water-level
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/components/dashboard/DashboardPanel.tsx
  - frontend/components/dashboard/KocherGaugeWidget.tsx
  - frontend/components/map/KocherLayer.tsx
  - frontend/components/map/KocherPopup.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/components/sidebar/WaterLegend.tsx
  - frontend/app/page.tsx
  - frontend/hooks/useKpi.ts
autonomous: true
requirements: [REQ-KOCHER-02, REQ-KOCHER-03, REQ-KOCHER-04, REQ-KOCHER-05]

must_haves:
  truths:
    - "Dashboard shows Kocher gauge widget with current cm reading and color-coded bar"
    - "Widget shows trend arrow (rising/falling/stable) and sparkline of last 7 days"
    - "Warning badge appears when flood stage >= 1"
    - "Map shows Kocher river line colored by warning stage"
    - "Gauge pin at Huttlingen shows level readout on map"
  artifacts:
    - path: "frontend/components/dashboard/KocherGaugeWidget.tsx"
      provides: "Kocher gauge widget with color bar, trend arrow, sparkline"
      min_lines: 40
    - path: "frontend/components/map/KocherLayer.tsx"
      provides: "Kocher river line on map colored by warning stage"
      min_lines: 30
    - path: "frontend/components/map/KocherPopup.tsx"
      provides: "Popup for Kocher gauge pin click"
      min_lines: 20
  key_links:
    - from: "frontend/components/dashboard/KocherGaugeWidget.tsx"
      to: "/api/kpi"
      via: "useKpi hook water field"
      pattern: "data\\.water"
    - from: "frontend/components/dashboard/KocherGaugeWidget.tsx"
      to: "/api/timeseries"
      via: "useTimeseries hook for sparkline"
      pattern: "useTimeseries.*water"
    - from: "frontend/components/map/KocherLayer.tsx"
      to: "/api/layers/water"
      via: "waterData prop from useLayerData"
      pattern: "source_id.*lhp"
    - from: "frontend/components/dashboard/DashboardPanel.tsx"
      to: "frontend/components/dashboard/KocherGaugeWidget.tsx"
      via: "import and render"
      pattern: "KocherGaugeWidget"
    - from: "frontend/app/page.tsx"
      to: "frontend/components/map/KocherLayer.tsx"
      via: "MapView prop kocher"
      pattern: "kocher"
---

<objective>
Add Kocher water level frontend: dashboard gauge widget with color-coded bar, trend arrow, sparkline; map layer showing Kocher river line colored by warning stage with gauge pin popup.

Purpose: Gives citizens a clear visual indicator of Kocher river conditions and flood risk.
Output: KocherGaugeWidget, KocherLayer, KocherPopup, sidebar toggle, wiring into page state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-kocher-water-level/12-CONTEXT.md
@.planning/phases/12-kocher-water-level/12-01-SUMMARY.md

@frontend/components/dashboard/DashboardPanel.tsx
@frontend/components/dashboard/KpiTile.tsx
@frontend/components/dashboard/TrendArrow.tsx
@frontend/components/dashboard/TimeSeriesChart.tsx
@frontend/components/map/WaterLayer.tsx
@frontend/components/map/TrafficFlowLayer.tsx
@frontend/components/map/MapView.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/components/sidebar/WaterLegend.tsx
@frontend/app/page.tsx
@frontend/types/kpi.ts
@frontend/hooks/useKpi.ts

<interfaces>
<!-- Key types from Plan 01 output -->

From frontend/types/kpi.ts (added by Plan 01):
```typescript
export interface WaterKPI {
  level_cm: number | null
  flow_m3s: number | null
  stage: number | null  // 0-4 warning stage
  trend: 'rising' | 'falling' | 'stable' | null
  gauge_name: string | null
  last_updated: string | null
}

export interface KPIResponse {
  // ... existing fields ...
  water: WaterKPI | null
}
```

From frontend/components/dashboard/KpiTile.tsx:
```typescript
interface KpiTileProps {
  domain: string
  label: string
  icon: React.ReactNode
  value: string | null
  unit: string
  trend: number | null
  active: boolean
  onSelect: (domain: string) => void
  children?: React.ReactNode  // slot for sub-content
}
```

From frontend/components/dashboard/TrendArrow.tsx:
```typescript
interface TrendArrowProps {
  percent: number | null  // null = no trend
}
// Shows TrendingUp/TrendingDown icons with percentage
```

From frontend/components/map/WaterLayer.tsx:
```typescript
interface WaterLayerProps {
  data: LayerResponse | null;
  visible: boolean;
  lubwEnvVisible?: boolean;
}
// Uses Source id="water-features" with GeoJSON data
// Filters by source_id containing "pegelonline"
```

From frontend/hooks/useTimeseries.ts (existing):
```typescript
// useTimeseries(domain: string, from: Date, to: Date, town?: string)
// Returns { data, loading, error } where data has points[]
```

From /api/layers/water response features:
```
Feature properties include: source_id, station_name, river, level_cm, flow_m3s, stage, trend, attribution
LHP features have source_id starting with "lhp:"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KocherGaugeWidget, KocherLayer, and KocherPopup components</name>
  <files>frontend/components/dashboard/KocherGaugeWidget.tsx, frontend/components/map/KocherLayer.tsx, frontend/components/map/KocherPopup.tsx</files>
  <read_first>
    - frontend/components/dashboard/KpiTile.tsx (widget pattern)
    - frontend/components/dashboard/TrendArrow.tsx (trend display)
    - frontend/components/dashboard/TimeSeriesChart.tsx (sparkline reference)
    - frontend/components/map/WaterLayer.tsx (water layer pattern)
    - frontend/components/map/TrafficFlowLayer.tsx (line layer pattern)
    - frontend/components/map/TrafficFlowPopup.tsx (popup pattern)
    - frontend/types/kpi.ts (WaterKPI type from Plan 01)
    - frontend/hooks/useKpi.ts (understand data shape)
    - frontend/AGENTS.md (Next.js version warnings)
  </read_first>
  <action>
    1. Create frontend/components/dashboard/KocherGaugeWidget.tsx:
       - Props: `water: WaterKPI | null` (from KPI response)
       - Display current level in cm as large number
       - Color-coded horizontal bar below the number:
         - Green (#4CAF50) for level < 80cm
         - Yellow (#FFC107) for 80-120cm
         - Orange (#FF9800) for 120-160cm
         - Red (#F44336) for > 160cm
       - Bar should be a div with rounded corners, width proportional to level (cap at 200cm for bar width), background color based on thresholds above
       - Trend indicator: show arrow icon (TrendingUp from lucide for rising, TrendingDown for falling, Minus for stable) with text "steigend"/"fallend"/"stabil" in German
       - DO NOT use TrendArrow component (that shows percentage, not direction) — create inline trend display
       - Sparkline: use Recharts `<AreaChart>` mini version (height 40px, no axes, no grid) showing last 7 days of water_readings. Fetch via useTimeseries('water', sevenDaysAgo, now). Line color matches current level color.
       - Warning badge: if stage >= 1, show a Badge component (from shadcn/ui) with text "Warnstufe {stage}" in orange/red based on stage level. Stage 1-2 = orange badge, stage 3-4 = red badge.
       - If water is null, show "Keine Daten" muted text
       - Label: "Kocher Pegel" with Droplets icon from lucide-react
       - Wrap in shadcn Card component like KpiTile does

    2. Create frontend/components/map/KocherLayer.tsx:
       - Props: `data: LayerResponse | null, visible: boolean`
       - Filter features by source_id containing "lhp:" (separate from pegelonline features in same water domain)
       - Gauge pin: CircleLayerSpecification for the Huttlingen gauge point, larger circle (radius 10), color based on warning_stage property:
         - Stage 0: #1565C0 (blue, normal)
         - Stage 1: #FFC107 (yellow)
         - Stage 2: #FF9800 (orange)
         - Stage 3-4: #F44336 (red)
         Use data-driven styling with step expression on ['get', 'warning_stage']
       - Label: SymbolLayerSpecification showing level_cm text below the circle (like water-labels in WaterLayer)
       - Kocher river line: LineLayerSpecification with a static GeoJSON LineString representing the Kocher river through Aalen. Color matches warning stage. Use a hardcoded GeoJSON geometry for the Kocher river path from Oberkochen through Aalen to Huttlingen (approximate coordinates):
         ```
         [[10.1045, 48.7853], [10.0900, 48.7920], [10.0750, 48.8000], [10.0650, 48.8100],
          [10.0550, 48.8150], [10.0450, 48.8180], [10.0350, 48.8200], [10.0250, 48.8180],
          [10.0150, 48.8130], [10.0050, 48.8080], [9.9950, 48.8050]]
         ```
         Line width 4, opacity 0.7, color driven by warning_stage from the LHP feature properties.
       - Use a SEPARATE source (id="kocher-features") that filters the water data to only LHP features, to avoid interfering with existing water-features source
       - River line uses a second source (id="kocher-river") with the static GeoJSON LineString

    3. Create frontend/components/map/KocherPopup.tsx:
       - Props: `feature: GeoJSON.Feature`
       - Display: station name (from properties.station_name), river name, current level in cm, flow in m3/s, warning stage with colored badge, trend direction, attribution
       - German labels: "Wasserstand", "Abfluss", "Warnstufe", "Trend"
       - Format: similar to TrafficFlowPopup structure with label-value rows
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && ls frontend/components/dashboard/KocherGaugeWidget.tsx frontend/components/map/KocherLayer.tsx frontend/components/map/KocherPopup.tsx && grep -q "KocherGaugeWidget" frontend/components/dashboard/KocherGaugeWidget.tsx && grep -q "KocherLayer" frontend/components/map/KocherLayer.tsx && grep -q "KocherPopup" frontend/components/map/KocherPopup.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export.*function KocherGaugeWidget\|export default function KocherGaugeWidget" frontend/components/dashboard/KocherGaugeWidget.tsx
    - grep -q "stage.*>= 1\|stage >= 1\|warning" frontend/components/dashboard/KocherGaugeWidget.tsx (warning badge logic)
    - grep -qE "(steigend|fallend|stabil)" frontend/components/dashboard/KocherGaugeWidget.tsx (German trend labels)
    - grep -q "AreaChart\|LineChart" frontend/components/dashboard/KocherGaugeWidget.tsx (sparkline)
    - grep -q "lhp" frontend/components/map/KocherLayer.tsx (LHP feature filter)
    - grep -q "warning_stage\|stage" frontend/components/map/KocherLayer.tsx (stage-based coloring)
    - grep -q "LineString\|line" frontend/components/map/KocherLayer.tsx (river line geometry)
    - grep -q "Wasserstand" frontend/components/map/KocherPopup.tsx
  </acceptance_criteria>
  <done>KocherGaugeWidget shows level with color bar, trend, sparkline, and warning badge. KocherLayer renders gauge pin and river line colored by stage. KocherPopup shows gauge details in German.</done>
</task>

<task type="auto">
  <name>Task 2: Wire KocherGaugeWidget and KocherLayer into page, sidebar, and dashboard</name>
  <files>frontend/components/dashboard/DashboardPanel.tsx, frontend/components/map/MapView.tsx, frontend/components/sidebar/Sidebar.tsx, frontend/components/sidebar/WaterLegend.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/app/page.tsx (layer state management pattern)
    - frontend/components/map/MapView.tsx (layer rendering and popup routing)
    - frontend/components/sidebar/Sidebar.tsx (layer toggle pattern)
    - frontend/components/sidebar/WaterLegend.tsx (water legend)
    - frontend/components/dashboard/DashboardPanel.tsx (KPI tile rendering)
    - frontend/components/dashboard/KocherGaugeWidget.tsx (component created in Task 1)
    - frontend/components/map/KocherLayer.tsx (component created in Task 1)
    - frontend/components/map/KocherPopup.tsx (component created in Task 1)
    - frontend/hooks/useKpi.ts (check how KPI data is accessed)
    - frontend/AGENTS.md (Next.js version warnings)
  </read_first>
  <action>
    1. Update frontend/components/dashboard/DashboardPanel.tsx:
       - Import KocherGaugeWidget from './KocherGaugeWidget'
       - Import Droplets from 'lucide-react'
       - Add KocherGaugeWidget below the existing KPI tiles (after demographics, before Separator):
         ```tsx
         {data?.water != null && (
           <KocherGaugeWidget water={data.water} />
         )}
         ```
       - This uses the `water` field from KPIResponse (added by Plan 01)

    2. Update frontend/components/map/MapView.tsx:
       - Import KocherLayer from './KocherLayer'
       - Import KocherPopup from './KocherPopup'
       - Add prop: `kocherVisible?: boolean` to MapViewProps
       - Render KocherLayer inside the Map component, passing waterData filtered for LHP features, and kocherVisible for visibility. Actually, KocherLayer does its own filtering from waterData, so pass waterData and kocherVisible:
         ```tsx
         <KocherLayer data={waterData ?? null} visible={kocherVisible ?? false} />
         ```
       - Add 'kocher-gauge' and 'kocher-river-line' to interactiveLayerIds array
       - Add popup routing for kocher domain: when clicked layer starts with 'kocher', set domain to 'kocher' in PopupInfo
       - Add KocherPopup rendering in popup section:
         ```tsx
         {popupInfo.domain === 'kocher' && <KocherPopup feature={popupInfo.feature} />}
         ```
       - Add 'kocher' to PopupInfo domain union type

    3. Update frontend/components/sidebar/Sidebar.tsx:
       - Add `kocher?: boolean` to layerVisibility interface
       - Add 'kocher' to onToggleLayer union type
       - Add a toggle in the "Ebenen" section (near the water toggle):
         ```tsx
         <LayerToggle
           id="kocher-toggle"
           label="Kocher Pegel (LHP)"
           checked={layerVisibility.kocher ?? false}
           onCheckedChange={() => onToggleLayer('kocher')}
         />
         ```
       - Place it right after the "Pegel & Gewaesser" toggle

    4. Update frontend/components/sidebar/WaterLegend.tsx:
       - Add a Kocher section to the water legend showing the stage color scale:
         - Blue circle: "Normal (Stufe 0)"
         - Yellow circle: "Vorwarnung (Stufe 1)"
         - Orange circle: "Meldepegel (Stufe 2)"
         - Red circle: "Hochwasser (Stufe 3-4)"
         - Blue line swatch: "Kocher Flusslauf"
       - Only show when relevant (can always show since it's part of water legend)

    5. Update frontend/app/page.tsx:
       - Add `kocher: state.layers.includes('kocher')` to layerVisibility object
       - Add `kocher: 'kocher'` to LAYER_KEYS
       - Pass `kocherVisible={layerVisibility.kocher}` to MapView component
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform && grep -q "KocherGaugeWidget" frontend/components/dashboard/DashboardPanel.tsx && grep -q "KocherLayer" frontend/components/map/MapView.tsx && grep -q "KocherPopup" frontend/components/map/MapView.tsx && grep -q "kocher" frontend/components/sidebar/Sidebar.tsx && grep -q "kocher" frontend/app/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "KocherGaugeWidget" frontend/components/dashboard/DashboardPanel.tsx
    - grep -q "data?.water" frontend/components/dashboard/DashboardPanel.tsx
    - grep -q "KocherLayer" frontend/components/map/MapView.tsx
    - grep -q "KocherPopup" frontend/components/map/MapView.tsx
    - grep -q "kocherVisible" frontend/components/map/MapView.tsx
    - grep -q "kocher" frontend/components/sidebar/Sidebar.tsx
    - grep -q "kocher.*toggle\|kocher-toggle" frontend/components/sidebar/Sidebar.tsx
    - grep -q "kocher" frontend/app/page.tsx
    - grep -q "Stufe\|Warnstufe\|stage" frontend/components/sidebar/WaterLegend.tsx (stage legend added)
  </acceptance_criteria>
  <done>KocherGaugeWidget renders in dashboard when water KPI data available. KocherLayer toggleable in sidebar and renders on map. Kocher popup shows gauge details on click. Water legend includes stage color scale.</done>
</task>

</tasks>

<verification>
- Dashboard shows Kocher gauge widget with color-coded level bar
- Trend arrow shows rising/falling/stable
- Sparkline renders 7-day water level history
- Warning badge appears when stage >= 1
- Sidebar has "Kocher Pegel (LHP)" toggle
- Map shows Kocher river line colored by warning stage
- Gauge pin at Huttlingen with level label
- Clicking gauge shows popup with Wasserstand, Abfluss, Warnstufe, Trend
- Water legend includes stage color scale
</verification>

<success_criteria>
- KocherGaugeWidget displays current cm, color bar, trend, sparkline, warning badge
- KocherLayer shows river line + gauge pin with stage coloring
- All wired into page state, sidebar toggle, dashboard panel
- No interference with existing WaterLayer (Pegelonline gauges)
</success_criteria>

<output>
After completion, create `.planning/phases/12-kocher-water-level/12-02-SUMMARY.md`
</output>
