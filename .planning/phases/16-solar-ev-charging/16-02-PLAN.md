---
phase: 16-solar-ev-charging
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - frontend/components/map/EvChargingLiveLayer.tsx
  - frontend/components/map/SolarGlowLayer.tsx
  - frontend/components/map/SolarPopup.tsx
  - frontend/components/map/EvChargingPopup.tsx
  - frontend/components/map/MapView.tsx
  - frontend/components/sidebar/Sidebar.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [REQ-SOLAR-02, REQ-SOLAR-03, REQ-EV-02, REQ-EV-03]

must_haves:
  truths:
    - "Solar-equipped buildings glow proportional to current output in 3D view"
    - "Click building popup shows potential vs installed vs current production"
    - "EV charger pins are colored green=available, red=occupied, gray=offline"
    - "EV charger icon size is proportional to power (11kW small, 150kW large)"
    - "Click EV popup shows connector types, operator, live status"
  artifacts:
    - path: "frontend/components/map/SolarGlowLayer.tsx"
      provides: "3D building glow layer for solar production"
      contains: "SolarGlowLayer"
    - path: "frontend/components/map/EvChargingLiveLayer.tsx"
      provides: "Live EV charger pins with status coloring and size scaling"
      contains: "EvChargingLiveLayer"
    - path: "frontend/components/map/SolarPopup.tsx"
      provides: "Solar building popup with potential/installed/current production"
      contains: "SolarPopup"
  key_links:
    - from: "frontend/components/map/SolarGlowLayer.tsx"
      to: "/api/layers/energy"
      via: "useLayerData hook fetching solar installations with current_output_kw"
      pattern: "useLayerData.*energy"
    - from: "frontend/components/map/EvChargingLiveLayer.tsx"
      to: "/api/layers/infrastructure?source=ocpdb"
      via: "useLayerData hook with source filter for live OCPDB data"
      pattern: "infrastructure"
    - from: "frontend/components/map/MapView.tsx"
      to: "SolarGlowLayer, EvChargingLiveLayer"
      via: "component import and rendering"
      pattern: "SolarGlowLayer|EvChargingLiveLayer"
    - from: "frontend/app/page.tsx"
      to: "solarGlow layer toggle"
      via: "layerVisibility state and LAYER_KEYS"
      pattern: "solarGlow"
---

<objective>
Create frontend components for live solar production visualization and live EV charger status display. SolarGlowLayer makes solar-equipped buildings glow in 3D view proportional to current output. EvChargingLiveLayer replaces static BNetzA pins with live status-colored, power-sized pins from OCPDB. Add popups, sidebar toggles, and MapView wiring for both layers.

Purpose: Frontend visualization for REQ-SOLAR-02/03 (building glow + popup) and REQ-EV-02/03 (status-colored, power-sized pins)
Output: Two new map layers with popups, sidebar toggles, and full MapView integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-solar-ev-charging/16-01-SUMMARY.md
@frontend/components/map/MapView.tsx
@frontend/components/map/EnergyLayer.tsx
@frontend/components/map/EnergyPopup.tsx
@frontend/components/map/InfrastructureLayer.tsx
@frontend/components/map/EvChargingPopup.tsx
@frontend/components/map/BuildingsLayer.tsx
@frontend/components/sidebar/Sidebar.tsx
@frontend/app/page.tsx
@frontend/hooks/useLayerData.ts

<interfaces>
<!-- Key types and contracts from backend (Plan 01 output) -->

Solar installation features (domain="energy", installation_type="solar_*") have additional properties after Plan 01:
```typescript
// Feature properties from SolarProductionConnector (added to existing MaStR features)
interface SolarFeatureProperties {
  installation_type: 'solar_rooftop' | 'solar_ground';
  capacity_kw: number;          // installed capacity from MaStR
  current_output_kw: number;    // computed live output (capacity * irradiance_factor)
  irradiance_factor: number;    // 0.0-1.0 from Bright Sky
  commissioning_year?: number;
  operator?: string;
  municipality?: string;
}
```

EV charging features (domain="infrastructure", category="ev_charging", source="ocpdb") have properties:
```typescript
interface EvChargingFeatureProperties {
  category: 'ev_charging';
  source: 'ocpdb';              // distinguishes from old BNetzA data
  status: 'AVAILABLE' | 'OCCUPIED' | 'INOPERATIVE' | 'UNKNOWN';
  power_kw: number;             // max power in kW
  power_class: 'ac_slow' | 'ac_fast' | 'dc_fast';
  operator: string;
  address: string;
  num_connectors: number;
  connector_types: string[];    // ["Type2", "CCS", ...]
  evse_count: number;
}
```

From frontend/hooks/useLayerData.ts:
```typescript
function useLayerData(domain: string, town: string, timestamp?: Date | null): {
  data: FeatureCollection | undefined;
  error: Error | undefined;
  isLoading: boolean;
}
```

MapView layer domain union type (needs extending):
```typescript
type PopupDomain = 'transit' | 'airQuality' | 'water' | 'traffic' | 'trafficFlow' |
  'autobahn' | 'energy' | 'community' | 'evCharging' | 'roadworks' | 'kocher' |
  'parking' | 'busPosition' | 'solarGlow';  // add solarGlow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SolarGlowLayer + SolarPopup + EvChargingLiveLayer</name>
  <files>
    frontend/components/map/SolarGlowLayer.tsx
    frontend/components/map/SolarPopup.tsx
    frontend/components/map/EvChargingLiveLayer.tsx
  </files>
  <read_first>
    frontend/components/map/EnergyLayer.tsx
    frontend/components/map/EnergyPopup.tsx
    frontend/components/map/BuildingsLayer.tsx
    frontend/components/map/InfrastructureLayer.tsx
    frontend/components/map/EvChargingPopup.tsx
    frontend/components/map/ParkingLayer.tsx
    frontend/hooks/useLayerData.ts
  </read_first>
  <action>
    1. Create frontend/components/map/SolarGlowLayer.tsx:
       - Props: { town: string; visible: boolean }
       - Use useLayerData('energy', town) to get all energy features
       - Filter features to only solar installations: installation_type starts with "solar_"
       - Use a separate GeoJSON Source with id="solar-glow" (avoid conflict with existing energy source)
       - Render as circle layer (id="solar-glow-points") with:
         * circle-color: data-driven from current_output_kw:
           - 0 kW: '#6b7280' (gray, no output)
           - >0 to 5 kW: '#fbbf24' (amber glow)
           - >5 to 20 kW: '#f59e0b' (orange glow)
           - >20 kW: '#ef4444' (bright red glow)
           Use ['interpolate', ['linear'], ['get', 'current_output_kw'], ...] expression
         * circle-radius: proportional to current_output_kw:
           ['interpolate', ['linear'], ['get', 'current_output_kw'], 0, 4, 10, 8, 50, 14, 200, 20]
         * circle-opacity: 0.8
         * circle-blur: 0.4 (soft glow effect)
         * circle-stroke-width: 0 (no stroke for glow aesthetic)
       - Include interactiveLayerIds in the returned layer IDs for popup support

    2. Create frontend/components/map/SolarPopup.tsx:
       - Props: { feature: Feature }
       - Display:
         * Title: "Solaranlage (Dach)" or "(Freiflaeche)" based on installation_type
         * "Installiert: {capacity_kw} kW" (from MaStR)
         * "Aktuelle Leistung: {current_output_kw} kW" (live computed)
         * "Auslastung: {Math.round(irradiance_factor * 100)}%"
         * If commissioning_year: "Inbetriebnahme: {year}"
       - Style: consistent with existing EnergyPopup (text-sm, max-w-[240px])

    3. Create frontend/components/map/EvChargingLiveLayer.tsx:
       - Props: { town: string; visible: boolean }
       - Fetch infrastructure data using useLayerData('infrastructure', town)
       - Filter features to only OCPDB chargers: source === "ocpdb"
       - Use separate GeoJSON Source with id="ev-charging-live" (separate from old infrastructure-ev)
       - Render as circle layer (id="ev-charging-live-points") with:
         * circle-color: data-driven from status property:
           ['match', ['get', 'status'],
             'AVAILABLE', '#22c55e',
             'OCCUPIED', '#ef4444',
             'INOPERATIVE', '#9ca3af',
             '#9ca3af'] (default gray for UNKNOWN)
         * circle-radius: proportional to power_kw (per REQ-EV-03):
           ['interpolate', ['linear'], ['get', 'power_kw'], 11, 6, 22, 8, 50, 10, 150, 14]
         * circle-stroke-width: 2
         * circle-stroke-color: '#ffffff'
       - No clustering (individual status visibility matters at all zoom levels)
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SolarGlowLayer" frontend/components/map/SolarGlowLayer.tsx
    - grep -q "solar-glow" frontend/components/map/SolarGlowLayer.tsx
    - grep -q "current_output_kw" frontend/components/map/SolarGlowLayer.tsx
    - grep -q "SolarPopup" frontend/components/map/SolarPopup.tsx
    - grep -q "Installiert" frontend/components/map/SolarPopup.tsx
    - grep -q "Aktuelle Leistung" frontend/components/map/SolarPopup.tsx
    - grep -q "EvChargingLiveLayer" frontend/components/map/EvChargingLiveLayer.tsx
    - grep -q "ev-charging-live" frontend/components/map/EvChargingLiveLayer.tsx
    - grep -q "AVAILABLE.*#22c55e" frontend/components/map/EvChargingLiveLayer.tsx
    - grep -q "power_kw" frontend/components/map/EvChargingLiveLayer.tsx
  </acceptance_criteria>
  <done>SolarGlowLayer renders solar installations with output-proportional glow, SolarPopup shows potential/installed/current, EvChargingLiveLayer renders status-colored power-sized pins</done>
</task>

<task type="auto">
  <name>Task 2: Wire layers into MapView, Sidebar, and page.tsx</name>
  <files>
    frontend/components/map/MapView.tsx
    frontend/components/map/EvChargingPopup.tsx
    frontend/components/sidebar/Sidebar.tsx
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/components/map/MapView.tsx
    frontend/components/sidebar/Sidebar.tsx
    frontend/app/page.tsx
    frontend/components/map/EvChargingPopup.tsx
  </read_first>
  <action>
    1. Update frontend/components/map/MapView.tsx:
       a. Add imports for SolarGlowLayer, SolarPopup, EvChargingLiveLayer at top
       b. Add props: solarGlowVisible?: boolean (default false)
       c. Add 'solarGlow' to the PopupInfo domain union type
       d. In the onClick handler, add layer ID detection:
          - layerId === 'solar-glow-points' -> domain = 'solarGlow'
          - layerId === 'ev-charging-live-points' -> domain = 'evCharging' (reuse existing domain)
       e. Add interactiveLayerIds entries: 'solar-glow-points', 'ev-charging-live-points'
       f. Render SolarGlowLayer inside Map:
          {solarGlowVisible && <SolarGlowLayer town={town} visible={true} />}
          Place after EnergyLayer rendering
       g. Render EvChargingLiveLayer inside Map:
          {evChargingVisible && <EvChargingLiveLayer town={town} visible={true} />}
          Place after InfrastructureLayer rendering (live pins overlay old static ones)
       h. In popup rendering section, add solarGlow case:
          popupInfo.domain === 'solarGlow' ? <SolarPopup feature={popupInfo.feature} /> : ...

    2. Update frontend/components/map/EvChargingPopup.tsx:
       - Add live status display fields from OCPDB data:
         * Status badge: green "Verfuegbar" / red "Belegt" / gray "Ausser Betrieb"
         * Power class label: "AC (11 kW)" / "AC (22 kW)" / "DC Schnellladen (150 kW)"
         * Connector types: "Steckertypen: Type 2, CCS"
         * EVSE count: "Ladepunkte: 2"
       - Keep backward compatible with old BNetzA data (check for source === "ocpdb" property)
       - If source === "ocpdb": show live status badge and OCPDB-specific fields
       - If source is not "ocpdb": show old BNetzA fields (existing behavior preserved)

    3. Update frontend/components/sidebar/Sidebar.tsx:
       a. Add solarGlow to layerVisibility interface: solarGlow?: boolean
       b. Add 'solarGlow' to onToggleLayer union type
       c. Add LayerToggle in the Energie group (after existing energy toggle):
          <LayerToggle id="solar-glow-toggle" label="Solar-Erzeugung (live)" checked={layerVisibility.solarGlow ?? false} onCheckedChange={() => onToggleLayer('solarGlow')} />
       d. Update the existing EV toggle label from "E-Ladesaeulen (BNetzA)" to "E-Ladesaeulen (live)"
          to reflect that we now show live OCPDB data

    4. Update frontend/app/page.tsx:
       a. Add solarGlow to layerVisibility object: solarGlow: state.layers.includes('solar_glow')
       b. Add to LAYER_KEYS: solarGlow: 'solar_glow'
       c. Pass solarGlowVisible={layerVisibility.solarGlow} to MapView
       d. Pass solarGlow to Sidebar's layerVisibility object
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SolarGlowLayer" frontend/components/map/MapView.tsx
    - grep -q "EvChargingLiveLayer" frontend/components/map/MapView.tsx
    - grep -q "SolarPopup" frontend/components/map/MapView.tsx
    - grep -q "solarGlow" frontend/components/map/MapView.tsx
    - grep -q "solar-glow-toggle" frontend/components/sidebar/Sidebar.tsx
    - grep -q "Solar-Erzeugung" frontend/components/sidebar/Sidebar.tsx
    - grep -q "solarGlow" frontend/app/page.tsx
    - grep -q "solar_glow" frontend/app/page.tsx
    - grep -q "Verfuegbar\|AVAILABLE" frontend/components/map/EvChargingPopup.tsx
  </acceptance_criteria>
  <done>Both layers wired into MapView with popup support, sidebar toggles added, page.tsx layer state connected, EV popup shows live status</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no type errors
- `grep -q "SolarGlowLayer" frontend/components/map/MapView.tsx` confirms layer wired
- `grep -q "EvChargingLiveLayer" frontend/components/map/MapView.tsx` confirms layer wired
- `grep -q "solar-glow-toggle" frontend/components/sidebar/Sidebar.tsx` confirms sidebar toggle
- `grep -q "solar_glow" frontend/app/page.tsx` confirms URL state integration
</verification>

<success_criteria>
- SolarGlowLayer renders solar installations with glow proportional to current_output_kw
- SolarPopup shows installed capacity, current production, and irradiance factor
- EvChargingLiveLayer renders OCPDB chargers with status-based colors and power-based sizes
- EvChargingPopup shows live status badge, power, connector types for OCPDB data
- Both layers togglable from sidebar with correct labels
- Clicking features opens correct popups
</success_criteria>

<output>
After completion, create `.planning/phases/16-solar-ev-charging/16-02-SUMMARY.md`
</output>
