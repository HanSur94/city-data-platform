---
phase: 05-dashboard
plan: 08
type: execute
wave: 6
depends_on:
  - 05-07
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - MAP-06
must_haves:
  truths:
    - "KPI tiles display without blank/crash at http://localhost:4000"
    - "Clicking a KPI tile opens the detail panel and highlights the tile ring"
    - "Date range presets change the chart data window"
    - "Time slider scrubs and map layers visually update (or show historical badge)"
    - "Copying the URL and pasting in new tab restores the same view"
    - "Layout has no horizontal scroll at 768px viewport width"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Fully integrated dashboard page"
      contains: "Suspense"
  key_links:
    - from: "KPI tile click"
      to: "activeDomain URL param"
      via: "update({ domain }) in page.tsx"
      pattern: "domain"
    - from: "TimeSlider scrub"
      to: "?at= backend param"
      via: "historicalTimestamp → useLayerData → fetchLayer(?at=)"
      pattern: "at"
---

<objective>
Human verification of the complete Phase 5 dashboard: KPI tiles, charts, time slider, detail panels, URL state, and responsive layout.

Purpose: All components were built and wired in Plans 01–07. This checkpoint confirms the full experience works end-to-end in a running browser session.

Output: Human sign-off that all 6 Phase 5 requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run build check</name>
  <files></files>
  <read_first>
    - frontend/app/page.tsx (confirm Suspense wrapper present)
  </read_first>
  <action>
Run from the frontend directory:

```bash
# 1. Type check (must pass with 0 errors before human verify)
cd /Users/hannessuhr/Projects/city-data-platform/frontend
pnpm exec tsc --noEmit

# 2. Start the frontend dev server (port 4000 per project decision)
# The server should already be running, or start it:
# pnpm dev
```

Confirm `tsc --noEmit` passes with 0 errors. Report any errors found before the checkpoint.

If there are TypeScript errors, fix them before requesting human verification.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && pnpm exec tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} test {} -eq 0 && echo "tsc PASS" || pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `pnpm exec tsc --noEmit` produces 0 TypeScript errors
    - Dev server is running or can be started
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 Dashboard: KPI tiles (AQI, Wetter, ÖPNV), time-series charts, date range picker,
    domain detail panels, time slider (historical map snapshots), and URL permalink state.
    Layout: Sidebar (280px) | Map (flex-1) + TimeSlider below | DashboardPanel (320px, right).
  </what-built>
  <how-to-verify>
    Start the dev server if not running: `cd /Users/hannessuhr/Projects/city-data-platform/frontend && pnpm dev`
    Then visit http://localhost:4000

    1. **DASH-01 — KPI tiles:**
       - Right panel shows 3 tiles: Luftqualität, Wetter, ÖPNV
       - Each tile has an icon + value (or "Kein aktueller Messwert") + unit
       - Panel header shows "Übersicht"; section header shows "Kennzahlen"

    2. **DASH-02 — Date range picker + charts:**
       - Below KPI tiles: "Verlauf" section with 3 preset buttons (24h, 7 Tage, 30 Tage) + Zeitraum...
       - Active preset button is filled; inactive are outline
       - Clicking "7 Tage" changes the chart date range
       - Clicking "Zeitraum..." opens a calendar popover with 2 months; selecting a range + "Übernehmen" closes popover

    3. **DASH-03 — Domain detail panel:**
       - Click the "Luftqualität" KPI tile → tile gets a border ring, chart area changes to "Luftqualität — Messverlauf" with a back button
       - Click "← Zurück" → returns to default chart + date picker
       - Click "ÖPNV" tile → shows "ÖPNV-Daten sind statisch (GTFS). Zeitverlauf ist nicht verfügbar."

    4. **MAP-06 — Time slider:**
       - Below the map: horizontal slider with "Live" label at rightmost position
       - Drag the slider to the left → label changes to "HH:mm Uhr", "Zurück zu Live" button appears
       - Map shows a historical timestamp badge overlay
       - Clicking "Zurück zu Live" resets slider to rightmost, badge disappears

    5. **DASH-04 — Permalink:**
       - Click a KPI tile, change date to "7 Tage", drag slider halfway
       - Copy the browser URL — it should contain `?domain=aqi&layers=...&from=...&to=...&ts=...`
       - Open the URL in a new tab → same tile selected, same date range, same slider position

    6. **DASH-05 — Responsive (768px):**
       - Open browser DevTools, set viewport to 768px wide
       - Dashboard panel (320px right side) should disappear (hidden on tablet)
       - Map should fill the full viewport width
       - No horizontal scrollbar

    Report any issues found for each numbered check.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 checks pass, or describe which checks failed and what you observed.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification of complete Phase 5 dashboard against all 6 requirements.
</verification>

<success_criteria>
All 6 checks pass:
1. KPI tiles render with German labels
2. Date range picker + charts work
3. Detail panel expands/collapses on tile click
4. Time slider scrubs, shows timestamp badge, "Zurück zu Live" works
5. URL encodes current view; pasting URL restores state
6. No horizontal scroll at 768px viewport
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-08-SUMMARY.md`
</output>
