---
phase: 05-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/components/ui/chart.tsx
  - frontend/components/ui/calendar.tsx
  - frontend/components/ui/popover.tsx
  - frontend/components/ui/slider.tsx
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "recharts 3.x is importable in the frontend"
    - "shadcn chart, calendar, popover, and slider components exist in components/ui/"
  artifacts:
    - path: "frontend/components/ui/chart.tsx"
      provides: "ChartContainer, ChartTooltip, ChartTooltipContent exports"
      contains: "ChartContainer"
    - path: "frontend/components/ui/calendar.tsx"
      provides: "Calendar component"
      contains: "Calendar"
    - path: "frontend/components/ui/popover.tsx"
      provides: "Popover, PopoverTrigger, PopoverContent exports"
      contains: "Popover"
    - path: "frontend/components/ui/slider.tsx"
      provides: "Slider component"
      contains: "Slider"
  key_links:
    - from: "TimeSeriesChart.tsx (future)"
      to: "components/ui/chart.tsx"
      via: "import { ChartContainer } from '@/components/ui/chart'"
      pattern: "ChartContainer"
---

<objective>
Install recharts and add the four required shadcn components (chart, calendar, popover, slider) so subsequent plans can import them without further setup.

Purpose: All chart and date-picker components in Phase 5 depend on these. They must exist before any dashboard component can be implemented.

Output: `recharts` in `package.json` + four new `components/ui/*.tsx` files generated by the shadcn CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/package.json
@frontend/components.json
</context>

<interfaces>
<!-- shadcn chart component API (from RESEARCH.md Pattern 2) -->
```tsx
import { ChartContainer, ChartTooltip, ChartTooltipContent } from '@/components/ui/chart'
// ChartContainer accepts: config (Record<string, {label, color}>), className
// Wraps Recharts charts and provides CSS variable color tokens

import { Calendar } from '@/components/ui/calendar'
// Calendar accepts: mode="range", selected, onSelect, numberOfMonths

import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover'
// Standard Radix popover pattern

import { Slider } from '@/components/ui/slider'
// Radix-based. Accepts: min, max, step, value (number[]), onValueChange
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts and add shadcn components</name>
  <files>
    frontend/package.json,
    frontend/components/ui/chart.tsx,
    frontend/components/ui/calendar.tsx,
    frontend/components/ui/popover.tsx,
    frontend/components/ui/slider.tsx
  </files>
  <read_first>
    - frontend/package.json (verify recharts not already installed)
    - frontend/components.json (confirm shadcn config — needed by npx shadcn add)
    - frontend/components/ui/ (list existing components to avoid re-adding)
  </read_first>
  <action>
Run in order from `frontend/` directory:

```bash
# Step 1: Install recharts
cd /Users/hannessuhr/Projects/city-data-platform/frontend
pnpm add recharts

# Step 2: Add shadcn components (non-interactive)
# chart wraps recharts + provides ChartContainer, ChartTooltip
npx shadcn@latest add chart --yes
npx shadcn@latest add calendar --yes
npx shadcn@latest add popover --yes
npx shadcn@latest add slider --yes
```

If any component already exists (from a prior run), shadcn will ask to overwrite — pass `--yes` or `--overwrite` flag to auto-confirm.

Do NOT modify the generated files — use them as-is. The shadcn CLI generates correct implementations.

After running, verify the files exist:
- `frontend/components/ui/chart.tsx`
- `frontend/components/ui/calendar.tsx`
- `frontend/components/ui/popover.tsx`
- `frontend/components/ui/slider.tsx`

Also verify `recharts` appears in `frontend/package.json` dependencies.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && node -e "require('./node_modules/recharts/index.js'); console.log('recharts OK')" && ls components/ui/chart.tsx components/ui/calendar.tsx components/ui/popover.tsx components/ui/slider.tsx && echo "shadcn components OK"</automated>
  </verify>
  <done>
    - `recharts` in `frontend/package.json` dependencies
    - `frontend/node_modules/recharts/` exists
    - All four `components/ui/*.tsx` files exist with correct exports
    - TypeScript build does not error on these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test imports compile</name>
  <files>frontend/types/dashboard-deps-check.ts</files>
  <read_first>
    - frontend/components/ui/chart.tsx (confirm exports — ChartContainer must be exported)
    - frontend/components/ui/slider.tsx (confirm Slider export)
  </read_first>
  <action>
Create a temporary type-check file `frontend/types/dashboard-deps-check.ts`:

```ts
// Compile-time check — delete after plan execution confirms types are valid
// This file is NOT imported anywhere — it is a tsc diagnostic tool only.
export type _ChartCheck = typeof import('../components/ui/chart')['ChartContainer']
export type _CalendarCheck = typeof import('../components/ui/calendar')['Calendar']
export type _PopoverCheck = typeof import('../components/ui/popover')['Popover']
export type _SliderCheck = typeof import('../components/ui/slider')['Slider']
```

Run `pnpm exec tsc --noEmit` and confirm no errors from these imports. Then DELETE this file (it was only for verification).

If the shadcn components generate with slightly different export names, adjust the check file accordingly and note the actual export names in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/frontend && pnpm exec tsc --noEmit 2>&1 | grep -v "dashboard-deps-check" | grep -c "error" | xargs -I{} test {} -eq 0 && echo "tsc clean" || echo "tsc errors found"</automated>
  </verify>
  <done>
    - `tsc --noEmit` produces 0 errors (or only pre-existing errors unrelated to new files)
    - Temp check file deleted after verification
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/hannessuhr/Projects/city-data-platform/frontend
ls components/ui/chart.tsx components/ui/calendar.tsx components/ui/popover.tsx components/ui/slider.tsx
grep '"recharts"' package.json
```
</verification>

<success_criteria>
- recharts installed and importable
- chart, calendar, popover, slider components exist in components/ui/
- No TypeScript errors from the new files
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-02-SUMMARY.md`
</output>
