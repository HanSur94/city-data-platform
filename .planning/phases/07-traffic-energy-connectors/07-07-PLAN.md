---
phase: 07-traffic-energy-connectors
plan: 07
type: execute
wave: 5
depends_on: ["07-05", "07-06"]
files_modified: []
autonomous: false
requirements:
  - TRAF-03
  - TRAF-04
  - TRAF-05
  - ENRG-01
  - ENRG-02
  - ENRG-03
  - ENRG-04

must_haves:
  truths:
    - "All 7 Phase 7 requirements visually verified in the browser"
    - "Traffic circles, Autobahn markers, and energy installations render on map"
    - "KPI tiles and detail panels display correct data"
    - "Full backend test suite passes"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: verify all Phase 7 requirements are working in the browser.

Purpose: Visual and functional verification that traffic and energy domains are fully operational.
Output: Verification sign-off or issue list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-traffic-energy-connectors/07-CONTEXT.md
@.planning/phases/07-traffic-energy-connectors/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start services</name>
  <files></files>
  <read_first>
    docker-compose.yml
  </read_first>
  <action>
    1. Run full backend test suite: `cd backend && pytest -x -q`
    2. Run frontend TypeScript check: `cd frontend && npx tsc --noEmit`
    3. Ensure docker-compose services are running: `docker-compose up -d`
    4. Wait for services to be healthy
    5. Run Alembic migration: `cd backend && alembic upgrade head`
    6. Verify connectors are registered: check scheduler logs for 5 new connector classes
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && pytest -x -q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0 with all tests passing
    - npx tsc --noEmit exits 0
    - docker-compose ps shows all services running
  </acceptance_criteria>
  <done>All tests pass, TypeScript compiles, services running with migration applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 7 requirements in browser</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for approval. All automation was completed in prior plans -- this task is human visual verification only.

    What was built:
    - 5 new backend connectors (BASt, Autobahn, MobiData BW, SMARD, MaStR)
    - Traffic and energy map layers with popups
    - Traffic and energy KPI tiles in dashboard
    - Energy generation mix stacked bar chart
    - Traffic and energy detail panels with time-series charts
    - Sidebar toggle groups for Verkehr and Energie

    How to verify -- open http://localhost:4000 in the browser:

    1. TRAF-03 (BASt traffic counts): Toggle "Verkehrszaehlstellen (BASt)" in the Verkehr sidebar group. Verify colored circles appear on the map near Aalen (green/yellow/red). Click a circle -- popup should show station name, road, Kfz/h, congestion level.

    2. TRAF-04 (Autobahn roadworks): Toggle "Autobahn-Stoerungen (A7/A6)". Verify markers appear on A7/A6. Click one -- popup should show roadwork/closure type, description, detour info.

    3. TRAF-05 (MobiData BW): MobiData BW data appears alongside BASt circles when traffic layer is active (same domain, different source).

    4. ENRG-01 (Electricity mix): Check the Energie KPI tile in the dashboard panel. It should show renewable % with a mini stacked bar showing the generation mix breakdown.

    5. ENRG-02 (MaStR renewables): Toggle "Erneuerbare Anlagen (MaStR)" in the Energie sidebar group. Verify clustered circles appear. Zoom in -- circles dissolve to individual installations color-coded: yellow=solar, blue=wind, green=battery. Click one -- popup shows type, capacity kW, year.

    6. ENRG-03 (Wholesale price): Click the Energie KPI tile. In the detail panel, verify the wholesale price line chart appears alongside the generation mix area chart.

    7. ENRG-04 (Energy stats): In the energy detail panel, verify the stacked area chart shows generation sources over time. Date range picker should update both charts.

    Also verify:
    - Traffic KPI tile shows "{N} aktiv" with flow status
    - Clicking Traffic tile opens detail panel with flow chart + roadworks list
    - Legends appear in sidebar when layers are toggled on
    - No console errors in browser dev tools
  </action>
  <verify>User types "approved" or describes issues.</verify>
  <acceptance_criteria>
    - User confirms all 7 requirements verified
    - No blocking visual or functional issues reported
  </acceptance_criteria>
  <done>All Phase 7 requirements verified and approved by user.</done>
</task>

</tasks>

<verification>
All 7 Phase 7 requirements verified visually in the browser.
</verification>

<success_criteria>
User approves all visual and functional verifications. No blocking issues found.
</success_criteria>

<output>
After completion, create `.planning/phases/07-traffic-energy-connectors/07-07-SUMMARY.md`
</output>
