---
phase: 07-traffic-energy-connectors
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/app/connectors/bast.py
  - backend/app/connectors/autobahn.py
  - backend/app/connectors/mobidata_bw.py
  - tests/connectors/test_bast.py
  - tests/connectors/test_autobahn.py
  - tests/connectors/test_mobidata_bw.py
autonomous: true
requirements:
  - TRAF-03
  - TRAF-04
  - TRAF-05

must_haves:
  truths:
    - "BASt connector fetches CSV, parses stations, upserts features, persists traffic_readings"
    - "Autobahn connector fetches A7+A6 roadworks/closures, filters within 50km of Aalen, upserts features"
    - "MobiData BW connector fetches BW traffic count CSV, shares normalize logic with BASt"
    - "All 3 connectors have passing unit tests"
  artifacts:
    - path: "backend/app/connectors/bast.py"
      provides: "BASt traffic count connector"
      contains: "class BastConnector"
    - path: "backend/app/connectors/autobahn.py"
      provides: "Autobahn roadworks connector"
      contains: "class AutobahnConnector"
    - path: "backend/app/connectors/mobidata_bw.py"
      provides: "MobiData BW connector"
      contains: "class MobiDataBWConnector"
  key_links:
    - from: "backend/app/connectors/bast.py"
      to: "backend/app/connectors/base.py"
      via: "BaseConnector inheritance + persist()"
      pattern: "class BastConnector\\(BaseConnector\\)"
    - from: "backend/app/connectors/autobahn.py"
      to: "backend/app/connectors/base.py"
      via: "BaseConnector inheritance + upsert_feature()"
      pattern: "class AutobahnConnector\\(BaseConnector\\)"
---

<objective>
Implement the 3 traffic-domain backend connectors: BASt permanent traffic count stations, Autobahn API roadworks/closures, and MobiData BW traffic/bike counts.

Purpose: These connectors ingest traffic data from 3 German federal open data sources into the features table and traffic_readings hypertable.
Output: 3 connector files + updated test files (GREEN state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-traffic-energy-connectors/07-CONTEXT.md
@.planning/phases/07-traffic-energy-connectors/07-RESEARCH.md
@.planning/phases/07-traffic-energy-connectors/07-01-SUMMARY.md

<interfaces>
<!-- BaseConnector pattern from pegelonline.py -->
From backend/app/connectors/pegelonline.py:
```python
class PegelonlineConnector(BaseConnector):
    async def fetch(self) -> bytes:
        async with httpx.AsyncClient(timeout=30.0) as client:
            resp = await client.get(url)
            resp.raise_for_status()
            return resp.content

    def normalize(self, raw: bytes, **kwargs) -> list[Observation]:
        data = json.loads(raw)
        return [Observation(feature_id=..., domain="water", values={...}, timestamp=...)]

    async def run(self) -> None:
        raw = await self.fetch()
        # upsert features first, then normalize, then persist
        for item in parsed:
            feature_id = await self.upsert_feature(source_id=..., domain=..., geometry_wkt=..., properties={...})
        observations = self.normalize(raw, feature_ids=self._feature_ids)
        await self.persist(observations)
        await self._update_staleness()
```

From backend/app/connectors/base.py:
```python
class BaseConnector(ABC):
    async def upsert_feature(self, source_id: str, domain: str, geometry_wkt: str, properties: dict) -> str:
        ...
    async def persist(self, observations: list[Observation]) -> None:
        ...
    async def _update_staleness(self) -> None:
        ...
```

BASt CSV columns (from RESEARCH.md, encoding windows-1252):
Station ID, road name, direction, lane count, vehicle_count_total (Kfz/h), vehicle_count_hgv (SV/h), speed_avg_kmh

Autobahn API (from RESEARCH.md):
GET https://verkehr.autobahn.de/o/autobahn/{road}/services/roadworks
Response: {"roadworks": [{"identifier": ..., "coordinate": {"lat": ..., "long": ...}, "title": ..., "subtitle": ..., "description": ..., "isBlocked": bool, "extent": ...}]}

Aalen center: lat=48.84, lon=10.09. 50km radius for Autobahn filter.
Aalen bbox + 20km buffer for BASt: approx lat 48.56-49.10, lon 9.77-10.42
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BASt + MobiData BW traffic count connectors</name>
  <files>
    backend/app/connectors/bast.py
    backend/app/connectors/mobidata_bw.py
    tests/connectors/test_bast.py
    tests/connectors/test_mobidata_bw.py
  </files>
  <read_first>
    backend/app/connectors/pegelonline.py
    backend/app/connectors/base.py
    tests/connectors/test_bast.py
    tests/connectors/test_mobidata_bw.py
    backend/app/connectors/uba.py
  </read_first>
  <behavior>
    - test_bast_csv_parse_returns_observations: Given sample BASt CSV text (windows-1252 encoded, semicolon-delimited), BastConnector.normalize() returns list[Observation] with domain="traffic", values containing vehicle_count_total, vehicle_count_hgv, speed_avg_kmh, congestion_level
    - test_bast_normalize_computes_congestion_level: Given vehicle_count_total=500 and capacity estimate (lanes*800), congestion_level="free" when ratio<0.5, "moderate" when 0.5-0.8, "congested" when >0.8
    - test_bast_feature_upsert_source_id_format: source_id follows pattern "bast:{station_id}"
    - test_mobidata_csv_parse: MobiData BW CSV (same format as BASt but BW-filtered) parses correctly
    - test_mobidata_normalize_shares_bast_format: Output Observation format matches BASt connector output
  </behavior>
  <action>
    Create `backend/app/connectors/bast.py`:
    - `class BastConnector(BaseConnector):`
    - `BAST_CSV_URL = "https://www.bast.de/DE/Verkehrstechnik/Fachthemen/v2-verkehrszaehlung/Daten/2023_1/Jawe2023.csv?__blob=publicationFile&v=13"` (latest annual hourly data; update URL at implementation time if newer available)
    - `async def fetch(self) -> bytes:` — GET the CSV URL with httpx, return content
    - `def normalize(self, raw: bytes, **kwargs) -> list[Observation]:` — decode with `encoding='windows-1252'`, parse CSV with semicolon delimiter, filter stations within Aalen bbox+20km (lat 48.56-49.10, lon 9.77-10.42 read from `self.config.config` or derived from town bbox), compute congestion_level from vehicle_count_total / (lane_count * 800), return Observations with domain="traffic"
    - `async def run(self) -> None:` — override run() to upsert features first (source_id=f"bast:{station_id}", domain="traffic", geometry from lat/lon columns), then normalize, persist, _update_staleness
    - Helper function `_compute_congestion(count: int, lanes: int) -> str` — returns "free"|"moderate"|"congested"

    Create `backend/app/connectors/mobidata_bw.py`:
    - `class MobiDataBWConnector(BaseConnector):`
    - `MOBIDATA_URL` — MobiData BW hourly traffic count CSV endpoint
    - Reuse the same CSV parsing logic as BASt (same column format per RESEARCH.md)
    - Import `_compute_congestion` from bast.py to share logic
    - Same run() override pattern: upsert features, normalize, persist

    Update test files from RED/skip to actual tests with mock data:
    - test_bast.py: provide sample CSV bytes, mock httpx, verify Observation output
    - test_mobidata_bw.py: provide BW CSV bytes, verify same output format
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && pytest tests/connectors/test_bast.py tests/connectors/test_mobidata_bw.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/connectors/bast.py contains "class BastConnector(BaseConnector)"
    - backend/app/connectors/bast.py contains "_compute_congestion"
    - backend/app/connectors/bast.py contains "encoding" (windows-1252 handling)
    - backend/app/connectors/bast.py contains 'domain="traffic"'
    - backend/app/connectors/mobidata_bw.py contains "class MobiDataBWConnector(BaseConnector)"
    - tests/connectors/test_bast.py has no pytest.skip calls remaining
    - tests/connectors/test_mobidata_bw.py has no pytest.skip calls remaining
    - pytest tests/connectors/test_bast.py exits 0
    - pytest tests/connectors/test_mobidata_bw.py exits 0
  </acceptance_criteria>
  <done>BASt and MobiData BW connectors parse CSV data, compute congestion levels, upsert features, persist to traffic_readings. All tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Autobahn API connector for roadworks and closures</name>
  <files>
    backend/app/connectors/autobahn.py
    tests/connectors/test_autobahn.py
  </files>
  <read_first>
    backend/app/connectors/pegelonline.py
    backend/app/connectors/base.py
    tests/connectors/test_autobahn.py
  </read_first>
  <behavior>
    - test_autobahn_roadworks_parse: Given mock Autobahn API JSON response with 3 roadworks, normalize returns features with identifier, coordinate, title, description, isBlocked fields in properties
    - test_autobahn_filter_by_distance: Given roadworks at various coordinates, only those within 50km of Aalen center (48.84, 10.09) are kept; haversine distance check
  </behavior>
  <action>
    Create `backend/app/connectors/autobahn.py`:
    - `class AutobahnConnector(BaseConnector):`
    - `AUTOBAHN_BASE = "https://verkehr.autobahn.de/o/autobahn"`
    - `ROADS = ["A7", "A6"]` — per CONTEXT.md decision
    - `AALEN_CENTER = (48.84, 10.09)` — center point for distance filtering
    - `MAX_DISTANCE_KM = 50` — per CONTEXT.md decision
    - `async def fetch(self) -> bytes:` — fetch roadworks + closures for both A7 and A6, combine results into JSON bytes. For each road: GET `{AUTOBAHN_BASE}/{road}/services/roadworks` and GET `{AUTOBAHN_BASE}/{road}/services/closures`
    - `def normalize(self, raw: bytes, **kwargs) -> list[Observation]:` — returns empty list (Autobahn data is features-only, not time-series)
    - Helper `_haversine(lat1, lon1, lat2, lon2) -> float` — returns distance in km
    - `async def run(self) -> None:` — override run():
      1. Fetch all roadworks+closures for A7 and A6
      2. Parse JSON, filter entries within 50km of AALEN_CENTER using haversine
      3. For each entry: `await self.upsert_feature(source_id=f"autobahn:{entry['identifier']}", domain="traffic", geometry_wkt=f"POINT({entry['coordinate']['long']} {entry['coordinate']['lat']})", properties={"title": entry["title"], "description": entry.get("description", ""), "is_blocked": entry.get("isBlocked", False), "type": "closure" if entry.get("isBlocked") else "roadwork", "extent": entry.get("extent", ""), "subtitle": entry.get("subtitle", "")})`
      4. No persist() call (features-only)
      5. Call `await self._update_staleness()`

    Update test_autobahn.py from RED to GREEN:
    - Mock httpx responses with sample Autobahn JSON
    - Verify coordinate filtering
    - Verify feature properties extraction
  </action>
  <verify>
    <automated>cd /Users/hannessuhr/Projects/city-data-platform/backend && pytest tests/connectors/test_autobahn.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/connectors/autobahn.py contains "class AutobahnConnector(BaseConnector)"
    - backend/app/connectors/autobahn.py contains "verkehr.autobahn.de"
    - backend/app/connectors/autobahn.py contains "_haversine"
    - backend/app/connectors/autobahn.py contains "A7" and "A6"
    - backend/app/connectors/autobahn.py contains 'domain="traffic"'
    - tests/connectors/test_autobahn.py has no pytest.skip calls remaining
    - pytest tests/connectors/test_autobahn.py exits 0
  </acceptance_criteria>
  <done>Autobahn connector fetches A7+A6 roadworks/closures, filters within 50km of Aalen, upserts features. Tests GREEN.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/connectors/test_bast.py tests/connectors/test_autobahn.py tests/connectors/test_mobidata_bw.py -x -q` all pass
- All 3 connector files importable: `python -c "from app.connectors.bast import BastConnector; from app.connectors.autobahn import AutobahnConnector; from app.connectors.mobidata_bw import MobiDataBWConnector"`
</verification>

<success_criteria>
3 traffic-domain connectors implemented and tested. BASt + MobiData BW persist to traffic_readings. Autobahn upserts features only. All tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-traffic-energy-connectors/07-02-SUMMARY.md`
</output>
